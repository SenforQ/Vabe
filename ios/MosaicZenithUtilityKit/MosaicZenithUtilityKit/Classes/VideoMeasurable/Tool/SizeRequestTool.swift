
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let show_intimateId:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","g","i","f","t"]
fileprivate let mainSystemName:String = "Liststyle white manager"

/*: "mf/chatRoom/giftList" :*/
fileprivate let notiBackgroundRecordMsg:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/"]
fileprivate let show_myFormat:String = "giftListdata more reply var"

/*: "live/giftList" :*/
fileprivate let k_needMsg:[Character] = ["l","i","v","e","/"]
fileprivate let appModelStr:String = "gmanagert"

/*: "party/giftList" :*/
fileprivate let const_fillValue:[Character] = ["p","a","r","t","y","/","g"]
fileprivate let app_byTouchData:String = "iftListphoto week"

/*: "IM/getMessageList" :*/
fileprivate let kFromContent:String = "add make make shareIM/ge"
fileprivate let show_rawMessage:String = "sageListneed self fill table view"

/*: "mf/business/sendMsg" :*/
fileprivate let kPathTableKey:[Character] = ["m","f","/","b","u","s"]
fileprivate let show_textUrl:String = "inevideo"
fileprivate let data_giftUrl:[Character] = ["/","s","e","n","d","M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let noti_labelMsg:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e"]
fileprivate let noti_sendRangeFormat:String = "ndMsgitem let lab"

/*: "mf/business/sendGift" :*/
fileprivate let user_addFormat:String = "after print outsidemf/b"
fileprivate let notiFromMessage:String = "make halfess/"
fileprivate let show_withId:[Character] = ["f","t"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let user_managerSinceModelMsg:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","G","i","f","t"]

/*: "live/sendGift" :*/
fileprivate let const_topTitle:String = "liimagee"
fileprivate let const_numberOriginTrueMsg:String = "snap item intervalGift"

/*: "party/sendGift" :*/
fileprivate let data_resignMsg:String = "party/sin if type"
fileprivate let mainResultId:String = "endGiftcustom input as succeed height"

/*: "mf/user/getChatInfo" :*/
fileprivate let constInsideValue:String = "mf/userange say"
fileprivate let k_imageId:[Character] = ["r","/","g","e","t","C","h","a"]
fileprivate let notiReloadData:[Character] = ["t","I","n","f","o"]

/*: "IM/getMessageUserInfoList" :*/
fileprivate let show_buttonColorTitle:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","U","s","e","r"]
fileprivate let kInfoKey:String = "from if type player nowInfo"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let appHiddenFormat:String = "mf/cruinfo style can extension true"
fileprivate let data_locationMsg:String = "sh/wlet return snap make player"
fileprivate let app_centerViewMessage:String = "hoLiindex intimate text self"

/*: "mf/crush/ignore" :*/
fileprivate let notiLabName:[Character] = ["m","f","/","c","r"]
fileprivate let constToUrl:String = "ush/add lab mini self frame"

/*: "mf/crush/like" :*/
fileprivate let notiModelContent:String = "mf/ctime let name"
fileprivate let userInsideFilterTitle:String = "scalee"

/*: "mf/user/getRelation" :*/
fileprivate let data_atMsg:[Character] = ["m","f","/","u","s","e","r","/","g","e","t"]
fileprivate let userDeviceDataId:String = "Relamessage text view to layer"

/*: "user/attention" :*/
fileprivate let notiSucceedModelKey:[UInt8] = [0x6e,0x6f,0x69,0x74,0x6e,0x65,0x74,0x74,0x61,0x2f,0x72,0x65,0x73,0x75]

/*: "user/removeAttention" :*/
fileprivate let data_managerFrameValue:String = "name view add selfuser/r"
fileprivate let data_outputId:String = "ntito"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let dataPushName:[UInt8] = [0x5f,0x7d,0x72,0x3b,0x68,0x3c,0x79,0x7d,0x6e,0x72,0x3c,0x6c,0x73,0x75,0x72,0x68,0x6f,0x3c,0x75,0x7a,0x3c,0x65,0x73,0x69,0x3c,0x7a,0x73,0x70,0x70,0x73,0x6b,0x3c,0x79,0x7d,0x7f,0x74,0x3c,0x73,0x68,0x74,0x79,0x6e,0x62]

				private func dataHome(tap num: UInt8) -> UInt8 {
					return num ^ 28
				}

/*: "follow Success" :*/
fileprivate let userColorPath:[Character] = ["f","o","l","l","o","w"]
fileprivate let kUpData:[Character] = [" ","S","u","c","c","e","s","s"]

/*: "unfollow Success" :*/
fileprivate let showIndexUrl:[Character] = ["u","n","f","o","l","l","o","w"," "]
fileprivate let noti_voiceTitle:String = "Successlabel case file"

/*: "translate/msg" :*/
fileprivate let show_normalId:[Character] = ["t","r","a","n","s","l","a","t","e","/","m","s","g"]

/*: "targetLng" :*/
fileprivate let show_sizeName:[UInt8] = [0xad,0xb8,0xab,0xbe,0xbc,0xad,0x95,0xb7,0xbe]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let notiLabId:String = "kit last bottom objectmf/g"
fileprivate let show_userPath:String = "make view let doetAut"
fileprivate let k_playerFormat:String = "tevalue"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let notiModelValue:[Character] = ["m","f","/","g","r"]
fileprivate let notiTitleDataMessage:[Character] = ["e","e","t","/","g","e","t","I","n"]
fileprivate let dataManagerMessage:String = "tiequalate"

/*: "mf/chatRoom/info" :*/
fileprivate let k_senseValue:String = "M"
fileprivate let main_afterText:String = "f/chaview view show self title"
fileprivate let dataAppName:String = "tonfo"

/*: "mf/chatRoom/list" :*/
fileprivate let show_inputImageValue:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let mainStopValue:String = "extension at sample letom/list"

/*: "roomId" :*/
fileprivate let noti_withName:[UInt8] = [0x64,0x49,0x6d,0x6f,0x6f,0x72]

/*: "needExtraInfo" :*/
fileprivate let constContentName:[UInt8] = [0x6f,0x66,0x6e,0x49,0x61,0x72,0x74,0x78,0x45,0x64,0x65,0x65,0x6e]

/*: "mf/chatRoom/check" :*/
fileprivate let app_readingPath:String = "mf/clet in background"
fileprivate let notiARawMsg:String = "om/cvertical attention"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let k_itemCellLabValue:[Character] = ["m","f","/","c","h","a"]
fileprivate let const_viewTitle:String = "tRoom/time label report"
fileprivate let kEqualData:String = "model array between pushQuit"

/*: "mf/chatRoom/members" :*/
fileprivate let show_infoMessage:String = "mode button resultmf/ch"
fileprivate let mainCellLengthFormat:String = "/mat var type"
fileprivate let appContentStr:String = "appmbapprs"

/*: "translate/text" :*/
fileprivate let dataMessageText:String = "TRAN"
fileprivate let noti_tapCommentHeightName:String = "in data/text"

/*: "mf/user/queryStatus" :*/
fileprivate let mainPhotoTargetVarMsg:String = "view date dynamic type viewmf/user/q"
fileprivate let appVoiceTitle:String = "UERY"

/*: "toUids" :*/
fileprivate let kRawName:[UInt8] = [0x12,0x9,0x33,0xf,0x2,0x15]

				private func addHeight(range num: UInt8) -> UInt8 {
					return num ^ 102
				}

/*: "call/getCallTab" :*/
fileprivate let mainFreshData:String = "to view pathcall/"
fileprivate let user_actualMessage:[Character] = ["T","a","b"]

/*: "mf/user/remark" :*/
fileprivate let data_cookieStr:[Character] = ["m","f","/","u","s","e","r","/","r","e","m"]
fileprivate let noti_cancelId:String = "contentrk"

/*: "content" :*/
fileprivate let k_offName:[UInt8] = [0x10,0x1c,0x1d,0x7,0x16,0x1d,0x7]

				private func researchLaboratory(equal num: UInt8) -> UInt8 {
					return num ^ 115
				}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SizeRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class SizeRequestTool: NSObject {
public class SizeRequestTool: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [BivouacCacheModel]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [BivouacCacheModel]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func psychologicalFeatureCompletion(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(show_intimateId) + String(mainSystemName.prefix(4)))
        //: reqModel.params = ["type": type]
        reqModel.params = ["type": type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func variation(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(notiBackgroundRecordMsg) + String(show_myFormat.prefix(8)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func zapEqual(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(k_needMsg) + appModelStr.replacingOccurrences(of: "manager", with: "if") + "List")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func toTarget(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (String(const_fillValue) + String(app_byTouchData.prefix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func completionDistributionListWire(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(kFromContent.suffix(5)) + "tMes" + String(show_rawMessage.prefix(8)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func giveaway(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(kPathTableKey) + show_textUrl.replacingOccurrences(of: "video", with: "ss") + String(data_giftUrl))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func turnAroundWithinCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(noti_labelMsg) + String(noti_sendRangeFormat.prefix(5)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func dismissUser(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(user_addFormat.suffix(4)) + "usin" + String(notiFromMessage.suffix(4)) + "sendGi" + String(show_withId))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func paramCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(user_managerSinceModelMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func aidVideo(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (const_topTitle.replacingOccurrences(of: "image", with: "v") + "/send" + String(const_numberOriginTrueMsg.suffix(4)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func param(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(data_resignMsg.prefix(7)) + String(mainResultId.prefix(7)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func userClick(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(constInsideValue.prefix(6)) + String(k_imageId) + String(notiReloadData))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func imageKey(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(show_buttonColorTitle) + String(kInfoKey.suffix(4)) + "List")
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<BivouacCacheModel>.deserialize(from: result as? Array) {
            if let datas = Array<BivouacCacheModel>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [BivouacCacheModel])
                completion(succeed, datas as! [BivouacCacheModel])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func doViewCompletion(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        dataFilterMsg.playCompletion(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func betweenConstraint(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(appHiddenFormat.prefix(6)) + String(data_locationMsg.prefix(4)) + String(app_centerViewMessage.prefix(4)) + "keMeList")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func ignore(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(notiLabName) + String(constToUrl.prefix(4)) + "ignore")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(notiModelContent.prefix(4)) + "rush/li" + userInsideFilterTitle.replacingOccurrences(of: "scale", with: "k"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func part(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(data_atMsg) + String(userDeviceDataId.prefix(4)) + "tion")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func study(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: notiSucceedModelKey.reversed(), encoding: .utf8)! : (String(data_managerFrameValue.suffix(6)) + "emoveAtte" + data_outputId.replacingOccurrences(of: "to", with: "on"))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
                    if ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        data_tabKey.statusWith(showMsg: String(bytes: dataPushName.map{dataHome(tap: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        data_tabKey.statusWith(showMsg: (String(userColorPath) + String(kUpData)).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    data_tabKey.statusWith(showMsg: (String(showIndexUrl) + String(noti_voiceTitle.prefix(7))).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                data_tabKey.heatherMixtureMoment(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func isoclinic(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (String(show_normalId))
        //: reqModel.params = ["msgId": msgId, "targetLng": PicMacroDefine.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = ["msgId": msgId, String(bytes: show_sizeName.map{$0^217}, encoding: .utf8)!: PicMacroDefine.confinementDoing(), "type": "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func imaginationImageWith(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(notiLabId.suffix(4)) + "reet/g" + String(show_userPath.suffix(5)) + "oGreetCon" + k_playerFormat.replacingOccurrences(of: "value", with: "nt"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func titleDestroy(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(notiModelValue) + String(notiTitleDataMessage) + dataManagerMessage.replacingOccurrences(of: "equal", with: "m") + "Photo")
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = ["toUid": toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func rangeInstance(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (k_senseValue.lowercased() + String(main_afterText.prefix(5)) + "tRoom/" + dataAppName.replacingOccurrences(of: "to", with: "i"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func signaling(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(show_inputImageValue) + String(mainStopValue.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func door(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: noti_withName.reversed(), encoding: .utf8)!: roomId, String(bytes: constContentName.reversed(), encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(app_readingPath.prefix(4)) + "hatRo" + String(notiARawMsg.prefix(4)) + "heck")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func listChangeChild(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(k_itemCellLabValue) + String(const_viewTitle.prefix(6)) + "joinOr" + String(kEqualData.suffix(4)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func account(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(show_infoMessage.suffix(5)) + "atRoom" + String(mainCellLengthFormat.prefix(2)) + appContentStr.replacingOccurrences(of: "app", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func duringRemove(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (dataMessageText.lowercased() + "slate" + String(noti_tapCommentHeightName.suffix(5)))
        //: reqModel.params = ["text": text]
        reqModel.params = ["text": text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func queryCompletion(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(mainPhotoTargetVarMsg.suffix(9)) + appVoiceTitle.lowercased() + "Status")
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: kRawName.map{addHeight(range: $0)}, encoding: .utf8)!: toUids, "type": 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func pastAction(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(mainFreshData.suffix(5)) + "getCall" + String(user_actualMessage))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func untilOf(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(data_cookieStr) + noti_cancelId.replacingOccurrences(of: "content", with: "a"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: k_offName.map{researchLaboratory(equal: $0)}, encoding: .utf8)!: content, "top": isTop, "toUid": toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
