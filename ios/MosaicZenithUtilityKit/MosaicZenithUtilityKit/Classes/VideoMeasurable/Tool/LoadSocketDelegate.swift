
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let data_documentSizeResponseValue:String = "SOCKEfit"
fileprivate let kEqualErrorName:String = "view title share_KEY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let userStartMsg:[UInt8] = [0xe2,0xde,0xd2,0xda,0xd4,0xe3,0xee,0xe5,0xd8,0xd3,0xd4,0xde,0xd2,0xd7,0xd0,0xe3,0xee,0xe1,0xd4,0xe0,0xe4,0xd4,0xe2,0xe3,0xd2,0xd0,0xdb,0xdb,0xee,0xdd,0xde,0xe3,0xd8,0xd5,0xd8,0xd2,0xd0,0xe3,0xd8,0xde,0xdd]

				fileprivate func namePlay(list num: UInt8) -> UInt8 {
					let value = Int(num) + 113
					if value > 255 {
						return UInt8(value - 256)
					} else {
						return UInt8(value)
					}
				}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let k_scopeMsg:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x45,0x45,0x52,0x43,0x53,0x54,0x41,0x4f,0x4c,0x46,0x5f,0x45,0x56,0x49,0x43,0x45,0x52]

/*: "isCalling" :*/
fileprivate let data_userId:[Character] = ["i","s","C","a","l","l","i","n","g"]
 
/*: "SOCKET 连接成功." :*/
fileprivate let data_startKey:[Character] = ["S","O","C","K","E","T"," ","连","接","\u{6210}","功"]
fileprivate let const_addData:[Character] = ["."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let app_labelTitle:[Character] = ["S","O","C","K","E","T"," ","连"]
fileprivate let main_collectionName:String = "接失\u{8d25},"

/*: "SOCKET 失败 :*/
fileprivate let main_awakeCurrentFileTitle:[Character] = ["S","O","C","K","E","T"]
fileprivate let show_componentMsg:String = " 失败"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let const_equalName:String = "content, 切换"
fileprivate let mainNetValue:String = "t\u{57df}名重新尝"
fileprivate let userStopId:String = "试，地image："

/*: "SOCKET 已关闭, code: :*/
fileprivate let constYouId:String = "SOCKET pull status to announcement"
fileprivate let userGroupData:String = "\u{5df2}"
fileprivate let user_titleFormat:String = "label, "

/*: , reason: :*/
fileprivate let mainErrorName:[Character] = [","," ","r","e","a","s","o","n",":"]

/*: &platform=iphone&version= :*/
fileprivate let appValueStr:String = "view label make lab collection&plat"
fileprivate let noti_messageTitle:String = "ipmanagerone"
fileprivate let app_fillName:String = "type key self page interaction&version="

/*: &packageId= :*/
fileprivate let show_toName:String = "&packof false"
fileprivate let noti_messageText:[Character] = ["a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let main_curBeActionPath:String = "1v1seraction height top user equal"
fileprivate let user_withLikeMsg:[Character] = ["v","e","r","."]

/*: .com:7766" :*/
fileprivate let data_reasonMsg:String = ".com:77index normal filter photo"
fileprivate let user_okTitle:[Character] = ["6","6"]

/*: "43.129.185.194:7767" :*/
fileprivate let app_matchKey:String = "4point.12"
fileprivate let user_backgroundId:[Character] = ["4",":","7","7","6","7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let notiItemText:[UInt8] = [0x2e,0xad,0x97,0xe9,0xb3,0x85,0xe5,0x91,0xbd,0xe7,0xad,0x96,0xe6,0x8c,0xbc,0xef,0xac,0x90,0xe5,0x91,0x9b,0xe7,0x64,0x65,0x67,0x6e,0x61,0x68,0x43,0x79,0x74,0x69,0x6c,0x69,0x62,0x61,0x68,0x63,0x61,0x65,0x72,0x9c,0xbb,0xe7,0x91,0xbd,0xe7,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "callHeartbeat" :*/
fileprivate let const_makeShowStr:[Character] = ["c","a","l","l","H","e"]
fileprivate let const_shareMessage:String = "arpagebeapage"

/*: "recievedDate" :*/
fileprivate let app_rawText:String = "reneede"
fileprivate let data_birthExplainMessage:String = "E"

/*: "liveHeartBeat" :*/
fileprivate let user_containerStr:String = "liveHexpress let"

/*: "🔗 关闭 socket" :*/
fileprivate let constReplyStr:String = "\u{0001f517} \u{5173}闭 "
fileprivate let show_makeErrorPath:[Character] = ["t"]

/*: "The user logs out" :*/
fileprivate let userToData:String = "The uleave case count any video"
fileprivate let kTopStr:String = "manager succeedogs o"
fileprivate let appAddData:[Character] = ["u","t"]

/*: "SOCKET  :*/
fileprivate let const_objectKey:String = "SOCKET to extra privacy to"

/*: 重连. 重试次数： :*/
fileprivate let kStyleUrl:String = "重"
fileprivate let mainLabelPath:String = "连. 重试\u{6b21}\u{6570}："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let user_resultMsg:String = "SimageCKET"
fileprivate let kFromPath:String = "要重新\u{8fde}接."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let noti_postKey:[Character] = ["S","O","C","K","E","T"," ","正","在","连","接"," ","\u{4e0d}","需","\u{8981}","重"]
fileprivate let userConsciousQuantityStorageValue:[Character] = ["新","连","接","."]

/*: "&resume=1" :*/
fileprivate let k_loadUrl:[Character] = ["&","r","e","s","u","m","e","=","1"]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let noti_modelMessage:String = "SOCKEcell"
fileprivate let const_needKey:[Character] = [" "]
fileprivate let k_offKey:String = "开\u{59cb}连接"

/*: "onNetDisabled" :*/
fileprivate let showModeName:String = "onNsession tip self"
fileprivate let data_colorName:String = "etDielse file make if"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let data_errorUrl:String = "SOpositionKET"
fileprivate let user_leadingFormat:String = "md: add profile"
fileprivate let constBlackMessage:String = "."

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let const_valueShadowUpFormat:[UInt8] = [0x81,0x9d,0x91,0x99,0x97,0x86,0xf2,0xa6,0xa0,0xa6,0xb1,0x36,0x6d,0x4f,0x34,0x66,0x69,0xb1,0xbf,0xb6,0xe8,0xf2,0xb1,0xb3,0xbe,0xbe,0x9a,0xb7,0xb3,0xa0,0xa6,0xb0,0xb7,0xb3,0xa6,0xfc]

				private func makeResult(sub num: UInt8) -> UInt8 {
					return num ^ 210
				}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let main_levelLabelValue:[UInt8] = [0xc1,0xdd,0xd1,0xd9,0xd7,0xc6,0xb2,0xfe,0xfb,0xe4,0xf7,0x76,0x2d,0xf,0x74,0x26,0x29,0xf1,0xff,0xf6,0xa8,0xb2,0xfe,0xfb,0xe4,0xf7,0xda,0xf7,0xf3,0xe0,0xe6,0xd0,0xf7,0xf3,0xe6,0xbc]

				private func appNo(first num: UInt8) -> UInt8 {
					return num ^ 146
				}

/*: "🔗 处理消息：cmdName: :*/
fileprivate let show_pageData:[Character] = ["\u{0001f517}"," ","处","理","消","息","：","c","m","d","N","a"]
fileprivate let user_maleText:[Character] = ["m","e",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let data_equalVideoValue:String = "SOCgiftET"
fileprivate let dataVisualRecordStr:String = "\u{5e38}消息\u{ff1a}"
fileprivate let const_sharedKey:String = "ame:type info"

/*: "onReceivedGift" :*/
fileprivate let userPathExtentTitle:[Character] = ["o","n","R","e","c","e","i","v","e"]
fileprivate let main_cropContent:String = "if case attribute bardGift"

/*: "onVideoMatch" :*/
fileprivate let appTouchMessage:String = "onViportrait self cur"
fileprivate let data_directionStr:String = "leadingtch"

/*: "onRequestCall" :*/
fileprivate let notiNorUrl:[Character] = ["o","n","R","e","q","u","e"]
fileprivate let user_labKey:[Character] = ["s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let mainCellEqualAspectData:[Character] = ["o","n","C","a","n","c"]
fileprivate let show_filterFormat:[Character] = ["e","l","C","a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let app_colorName:String = "count title left layer viewonRe"
fileprivate let dataTabUrl:String = "ALL"

/*: "onAcceptCall" :*/
fileprivate let app_changeMessage:String = "onAccepath maximum show"
fileprivate let show_topFlexibleMsg:[Character] = ["p","t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let appVideoStr:String = "onStopCvalue bar in touch"
fileprivate let appRedFormat:[Character] = ["a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let dataCenterStr:[Character] = ["f","r","e"]
fileprivate let user_listSectionTitle:String = "eCallTsex succeed color user listener"

/*: "mfCoin" :*/
fileprivate let appDropMsg:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let showBottomKey:String = "input"
fileprivate let dataTextValue:String = "nMisstype var"

/*: "acceptCall" :*/
fileprivate let appThroughStr:String = "section model normal title viewacceptC"
fileprivate let k_keyTitle:String = "aappear"

/*: "stopCall" :*/
fileprivate let app_labData:String = "stacross"

/*: "requestCall" :*/
fileprivate let data_requestHiddenPath:[Character] = ["r","e","q","u"]
fileprivate let app_shadowData:[Character] = ["e","s","t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let dataSizeValue:[UInt8] = [0xb3,0xa1,0x86,0x9e,0x81,0x9c,0x8d,0xc8,0x91,0x87,0x9d,0xc8,0x9c,0x87,0xc8,0x89,0xc8,0x9e,0x81,0x8c,0x8d,0x87,0xc8,0x8b,0x89,0x84,0x84,0xc6,0xc6,0xc6,0xb5]

				private func addEmpty(mixture num: UInt8) -> UInt8 {
					return num ^ 232
				}

/*: "[Invite you to a voice call...]" :*/
fileprivate let k_shareManagerStr:[UInt8] = [0x52,0x40,0x65,0x6d,0x60,0x6b,0x5c,0x17,0x70,0x66,0x6c,0x17,0x6b,0x66,0x17,0x58,0x17,0x6d,0x66,0x60,0x5a,0x5c,0x17,0x5a,0x58,0x63,0x63,0x25,0x25,0x25,0x54]

				fileprivate func hiddenClick(app num: UInt8) -> UInt8 {
					let value = Int(num) - 247
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "nickname" :*/
fileprivate let kAddText:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let app_countName:String = "headPreturn label image"
fileprivate let showSendTitle:[Character] = ["i","c"]

/*: "onCallResume" :*/
fileprivate let appStrengthMessage:[Character] = ["o","n","C","a"]
fileprivate let constOpFormat:[Character] = ["l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let data_momentCellStr:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let mainNameFormat:String = "refheritages"
fileprivate let kObjectValue:String = "equal view returneCall"

/*: "onCallMotivation" :*/
fileprivate let showModelModeText:String = "head user toonCall"
fileprivate let dataAttributeContent:String = "let return view makeMotiva"

/*: "onLiveWarning" :*/
fileprivate let mainMakePath:[Character] = ["o","n","L","i","v","e","W","a","r","n","i"]
fileprivate let noti_frameMsg:String = "NG"

/*: "reason" :*/
fileprivate let dataLocationId:String = "retimeso"
fileprivate let dataNameFormat:[Character] = ["n"]

/*: "onStopLive" :*/
fileprivate let const_playerValue:String = "image back request bottom varonSto"
fileprivate let noti_actionColorMsg:String = "pLivetrue center"

/*: "onSendProgramGift" :*/
fileprivate let appItemPath:[Character] = ["o","n","S","e","n","d","P","r","o","g"]
fileprivate let const_addKey:String = "fill targetramGift"

/*: "onGameFs" :*/
fileprivate let k_modelData:String = "err varonGam"
fileprivate let userMaxMsg:String = "mode section self keyeFs"

/*: "broadStatus" :*/
fileprivate let const_nameMessage:String = "type self normal at labbroadS"
fileprivate let noti_allPanCenterData:[Character] = ["t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let constKitUrl:[Character] = ["o","n","C","a","l","l","C","h","a"]
fileprivate let main_giftBarPath:[Character] = ["t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let userSizeMessage:String = "model factor contentonNot"
fileprivate let k_acrossFormat:String = "resultio"
fileprivate let showPathOfValue:[Character] = ["p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let app_valueClickData:[Character] = ["o","n","U","s","e","r","B","l"]
fileprivate let noti_modelValue:[Character] = ["o","c","k"]

/*: "freeCallWin" :*/
fileprivate let data_makePath:String = "manager to import allow tipfreeC"
fileprivate let main_addPopContent:String = "with"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LoadSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let main_pointBarPath = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let kHomeStr = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let constDeviceMsg = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let mainFormatData = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let main_netData = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let show_barLevelNetMsg = (data_documentSizeResponseValue.replacingOccurrences(of: "fit", with: "T") + "_ADDR" + String(kEqualErrorName.suffix(4)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let main_numberId = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let app_thirdKey = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let kStreamContent = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let user_jumpMessage = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let appScreenMsg = NSNotification.Name(rawValue: String(bytes: userStartMsg.map{namePlay(list: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let main_closeScaleUrl = NSNotification.Name(rawValue: String(bytes: k_scopeMsg.reversed(), encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol DistrictManagerErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func dataFeature(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol EliteMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func beToData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func projectionData(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol HostObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func financialAid(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func monologuize(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func socket(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func sovietSocialistRepublic(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol TitleObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func betweenText(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func tagData(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func putToDeath(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class LoadSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum TypeSkipWithHost: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currServerVC: AnyObject?
    var currServerVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: SizeLanguageManager.shared.direction == .rightToLeft ? 0 : user_barText,
        //: y: StatusBarHeight+190
        y: app_versionItemStr + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: SizeLanguageManager.shared.direction == .rightToLeft ? 0 : user_barText,
        //: y: StatusBarHeight+190
        y: app_versionItemStr + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: SizeLanguageManager.shared.direction == .rightToLeft ? 0 : user_barText,
        //: y: StatusBarHeight+500
        y: app_versionItemStr + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: DistrictManagerErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: EliteMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: HostObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: TitleObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isServer = false
    @objc dynamic var isServer = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = LoadSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(changedEqual(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(sessionClickAdd),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(data_userId)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ListKingfisherCompatible.shared as! ListKingfisherCompatible).selfDestroy()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isServer")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ListKingfisherCompatible.shared as! ListKingfisherCompatible).selfDestroy()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension LoadSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        TitleSongLogTool.dataRender(msg: (String(data_startKey) + String(const_addData)))
        //: sendPing()
        equalImpingeOn()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: k_replaceKey,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: show_barLevelNetMsg)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        lapCoveringTipVisualImage()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        TitleSongLogTool.dataRender(msg: (String(app_labelTitle) + main_collectionName + " error:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = ExamineedReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = ExamineedReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == mainFormatData {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = markRetry(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            TitleSongLogTool.dataRender(msg: (String(main_awakeCurrentFileTitle) + show_componentMsg.capitalized) + "\(mainFormatData)" + (const_equalName.replacingOccurrences(of: "content", with: "次") + "过Socke" + mainNetValue + userStopId.replacingOccurrences(of: "image", with: "址")) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.createGift(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.crossMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        TitleSongLogTool.dataRender(msg: (String(constYouId.prefix(7)) + userGroupData + user_titleFormat.replacingOccurrences(of: "label", with: "关闭") + "code:") + "\(code)" + (String(mainErrorName)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.searched(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == user_jumpMessage {
            //: return
            return
        }
        //: if let status = ExamineedReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = ExamineedReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.createGift(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func markRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = ExamineedReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = ExamineedReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = deleteBar()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: show_barLevelNetMsg)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = ExamineedReactiveCompatible.share.loginUid
        let uid = ExamineedReactiveCompatible.share.loginUid
        //: let sid = ExamineedReactiveCompatible.share.loginSessionId
        let sid = ExamineedReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)" + (String(appValueStr.suffix(5)) + "form=" + noti_messageTitle.replacingOccurrences(of: "manager", with: "h") + String(app_fillName.suffix(9))) + "\(main_barTitle)" + (String(show_toName.prefix(5)) + String(noti_messageText)) + "\(mainStatusHalfMessage)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func deleteBar() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return showBarName ? [(String(main_curBeActionPath.prefix(6)) + String(user_withLikeMsg)) + "\(k_descriptionName)" + (String(data_reasonMsg.prefix(7)) + String(user_okTitle))] : [(app_matchKey.replacingOccurrences(of: "point", with: "3") + "9.185.19" + String(user_backgroundId))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension LoadSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func changedEqual(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            TitleSongLogTool.dataRender(msg: String(bytes: notiItemText.reversed(), encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.searched(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.createGift(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func sessionClickAdd() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.createGift(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.sexDoingData()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension LoadSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func equalImpingeOn() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: main_pointBarPath, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.greetButton(info: ["cmd": "ping"])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func appearWith() {
        //: endTalkingPing()
        removePing()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: kHomeStr, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isServer || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currServerVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currServerVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isServer || (self.announcement()?.isKind(of: ComponentErrorDelegate.self) == false && self.announcement()?.isKind(of: SeedViewController.self) == false) && (self.currServerVC?.isKind(of: ComponentErrorDelegate.self) == false && self.currServerVC?.isKind(of: SeedViewController.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.greetButton(info: ["cmd": (String(const_makeShowStr) + const_shareMessage.replacingOccurrences(of: "page", with: "t"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func removePing() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func sexDoingData() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(app_rawText.replacingOccurrences(of: "need", with: "ci") + "vedDat" + data_birthExplainMessage.lowercased())] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < user_senseName {
                //: ExamineedReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                ExamineedReactiveCompatible.share.toDetail(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension LoadSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func coequal() {
        //: endLivePing()
        soccerPlayer()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: constDeviceMsg, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.greetButton(info: ["cmd": (String(user_containerStr.prefix(5)) + "eartBeat")])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func soccerPlayer() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension LoadSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func lapCoveringTipVisualImage() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = WithThen()
        //: let keyWindow = PicMacroDefine.getWindow()
        let keyWindow = PicMacroDefine.statusDown()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isServer
        self.containerView?.isHidden = !self.isServer
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func afterCurrentView() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard LiveManager.upShared().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard ListThen.withShared().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension LoadSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func searched(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (constReplyStr + "socke" + String(show_makeErrorPath)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: user_jumpMessage, reason: (String(userToData.prefix(5)) + "ser l" + String(kTopStr.suffix(5)) + String(appAddData)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        removePing()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            afterCurrentView()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func pendingBe() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = LoadSocketDelegate.shared.markRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.createGift(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func createGift(timeInterval: TimeInterval, type: TypeSkipWithHost) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            TitleSongLogTool.dataRender(msg: (String(const_objectKey.prefix(7))) + "\(type.rawValue)" + (kStyleUrl.capitalized + mainLabelPath) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.targetCreate(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func targetCreate(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                TitleSongLogTool.dataRender(msg: (user_resultMsg.replacingOccurrences(of: "image", with: "O") + " 连接\u{6b63}常 \u{4e0d}需" + kFromPath))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                TitleSongLogTool.dataRender(msg: (String(noti_postKey) + String(userConsciousQuantityStorageValue)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isServer {
        if self.isServer {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(k_loadUrl)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending("&lang=\(RecordAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        searched(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = main_netData
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        TitleSongLogTool.dataRender(msg: (noti_modelMessage.replacingOccurrences(of: "cell", with: "T") + String(const_needKey) + k_offKey + ", \u{5730}址：") + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func greetButton(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if ExamineedReactiveCompatible.share.reachability?.connection == nil || ExamineedReactiveCompatible.share.reachability?.connection == .unavailable {
        if ExamineedReactiveCompatible.share.reachability?.connection == nil || ExamineedReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = ["cmd": (String(showModeName.prefix(3)) + String(data_colorName.prefix(4)) + "sabled"),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       "errno": "\(kStreamContent)",
                                       //: "data": [:]]
                                       "data": [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            crossMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info!["cmd"] as? String == "ping" {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                TitleSongLogTool.dataRender(msg: (data_errorUrl.replacingOccurrences(of: "position", with: "C") + " 连\u{63a5}保活c" + String(user_leadingFormat.prefix(4)) + "ping" + constBlackMessage.capitalized))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info!["cmd"] as? String == (String(const_makeShowStr) + const_shareMessage.replacingOccurrences(of: "page", with: "t")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                TitleSongLogTool.dataRender(msg: String(bytes: const_valueShadowUpFormat.map{makeResult(sub: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info!["cmd"] as? String == (String(user_containerStr.prefix(5)) + "eartBeat") {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                TitleSongLogTool.dataRender(msg: String(bytes: main_levelLabelValue.map{appNo(first: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info!["cmd"] as? String == "ping" {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = ["cmd": "onP",
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           "errno": "\(main_numberId)",
                                           //: "data": [:]]
                                           "data": [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                crossMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.createGift(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func crossMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json["cmd"].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json["errno"].intValue
        //: let msg = json["msg"].stringValue
        let msg = json["msg"].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json["data"].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(show_pageData) + String(user_maleText)) + "\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        noneTitle(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            TitleSongLogTool.dataRender(msg: (data_equalVideoValue.replacingOccurrences(of: "gift", with: "K") + " 接收\u{5f02}" + dataVisualRecordStr + "cmdN" + String(const_sharedKey.prefix(4))) + "\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !ExamineedReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !ExamineedReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                SizeSearchWindowManager.shared.straitAndNarrow()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.dataFeature(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(userPathExtentTitle) + String(main_cropContent.suffix(5))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: mainChangeData, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(appTouchMessage.prefix(4)) + "deoM" + data_directionStr.replacingOccurrences(of: "leading", with: "a")) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.beToData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(notiNorUrl) + String(user_labKey)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.projectionData(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(mainCellEqualAspectData) + String(show_filterFormat)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(app_colorName.suffix(4)) + "fuseC" + dataTabUrl.lowercased()) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(app_changeMessage.prefix(6)) + String(show_topFlexibleMsg)) { // 接收方发送acceptCall回执
            //: self.isServer = true
            self.isServer = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            appearWith()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.tagData(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(appVideoStr.prefix(7)) + String(appRedFormat)) { // 接收方发送stopCall回执
            //: self.isServer = false
            self.isServer = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            removePing()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(dataCenterStr) + String(user_listSectionTitle.prefix(6)) + "imes")] as? Int {
                //: ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: ExamineedReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            ExamineedReactiveCompatible.share.loginUserMode.mf_coin = json["data"][(String(appDropMsg))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: data_statusSystemName, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ListKingfisherCompatible.shared as! ListKingfisherCompatible).femaleTableAnnalsReport()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (showBottomKey.replacingOccurrences(of: "input", with: "o") + String(dataTextValue.prefix(5)) + "Coin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.sovietSocialistRepublic(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(appThroughStr.suffix(7)) + k_keyTitle.replacingOccurrences(of: "appear", with: "ll")) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isServer = true
            self.isServer = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            appearWith()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.financialAid(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (app_labData.replacingOccurrences(of: "across", with: "op") + "Call") { // 接收方/拨打方 挂断了通话
            //: self.isServer = false
            self.isServer = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            removePing()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.monologuize(data: data)
            // 更新余额
            //: ExamineedReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            ExamineedReactiveCompatible.share.loginUserMode.mf_coin = json["data"][(String(appDropMsg))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            expressionInsideMetadata(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: data_statusSystemName, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ListKingfisherCompatible.shared as! ListKingfisherCompatible).femaleTableAnnalsReport()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(data_requestHiddenPath) + String(app_shadowData)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: appScreenMsg, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: ExamineedReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                ExamineedReactiveCompatible.share.toDetail(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(app_rawText.replacingOccurrences(of: "need", with: "ci") + "vedDat" + data_birthExplainMessage.lowercased())] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: dataSizeValue.map{addEmpty(mixture: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json["data"]["type"].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: k_shareManagerStr.map{hiddenClick(app: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                DataTapThen.change(uid: String(json["data"]["uid"].intValue),
                                         //: title: data["nickname"] as? String,
                                         title: data[(String(kAddText))] as? String,
                                         //: body: body,
                                         body: body,
                                         //: imageUrl: data["headPic"] as? String)
                                         imageUrl: data[(String(app_countName.prefix(5)) + String(showSendTitle))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                DataTapThen.shared.no()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(appStrengthMessage) + String(constOpFormat)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isServer = true
            self.isServer = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            appearWith()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(appStrengthMessage) + String(constOpFormat))] = "1"
            //: ExamineedReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            ExamineedReactiveCompatible.share.reportCard(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (ListKingfisherCompatible.shared as! ListKingfisherCompatible).selfDestroy()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(data_momentCellStr)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isServer = false
            self.isServer = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            removePing()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.socket(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                DataTapThen.click(identifier: String(json["data"]["uid"].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                DataTapThen.shared.titleBgm()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (mainNameFormat.replacingOccurrences(of: "heritage", with: "u") + String(kObjectValue.suffix(5))) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isServer = false
            self.isServer = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            removePing()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.betweenText(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(showModelModeText.suffix(6)) + String(dataAttributeContent.suffix(6)) + "tion") { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.putToDeath?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(mainMakePath) + noti_frameMsg.lowercased()) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            LiveManager.upTable(content: msg, reason: json["data"][(dataLocationId.replacingOccurrences(of: "time", with: "a") + String(dataNameFormat))].stringValue, time: json["data"]["time"].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(const_playerValue.suffix(5)) + String(noti_actionColorMsg.prefix(5))) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            LiveManager.upShared().server()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(appItemPath) + String(const_addKey.suffix(7))) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.statusWith(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(k_modelData.suffix(5)) + String(userMaxMsg.suffix(3))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(const_nameMessage.suffix(6)) + String(noti_allPanCenterData))] as? Int ?? -1
            //: if ExamineedReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if ExamineedReactiveCompatible.share.appStatus == InstanceHashableRepresentation.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: main_closeScaleUrl, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: mainChangeData, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: main_closeScaleUrl, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(constKitUrl) + String(main_giftBarPath)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            SizeServerThen.eigenvalueOfASquareMatrixShared().bindTo(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(userSizeMessage.suffix(5)) + "ific" + k_acrossFormat.replacingOccurrences(of: "result", with: "at") + "nPopu" + String(showPathOfValue)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            SizeSearchWindowManager.shared.deleteSystem(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(app_valueClickData) + String(noti_modelValue)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            LiveManager.upShared().server()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ListThen.withShared().denomination()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: main_timeData, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension LoadSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func noneTitle(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(data_requestHiddenPath) + String(app_shadowData)) || cmdName == (String(notiNorUrl) + String(user_labKey)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if LiveManager.upShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                data_recordListContent = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                LiveManager.changeShape(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                LiveManager.upShared().server()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard data_recordListContent == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(data_momentCellStr)) || cmdName == (String(mainCellEqualAspectData) + String(show_filterFormat)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (mainNameFormat.replacingOccurrences(of: "heritage", with: "u") + String(kObjectValue.suffix(5))) || cmdName == (String(app_colorName.suffix(4)) + "fuseC" + dataTabUrl.lowercased()) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (app_labData.replacingOccurrences(of: "across", with: "op") + "Call") || cmdName == (String(appVideoStr.prefix(7)) + String(appRedFormat))
        {
            //: NEED_RESTORE_LIVE = false
            data_recordListContent = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = SectionView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                LiveManager.upShared().campOut()
            }
            //: alert.show()
            alert.byContent()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func expressionInsideMetadata(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(dataCenterStr) + String(user_listSectionTitle.prefix(6)) + "imes")] as? Int {
            //: ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(data_makePath.suffix(5)) + "allW" + main_addPopContent.replacingOccurrences(of: "with", with: "in"))] as? Bool
        //: if freeCallWin == true && ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = VoiceObjectProtocol()
            //: view.show()
            view.equalError()
            //: view.setShowData(data: data)
            view.withData(data: data)
        }
    }
}
