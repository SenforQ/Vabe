
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_equalUrl:[UInt8] = [0x20,0x25,0x20,0x2b,0xdf,0x1a,0x26,0x1b,0x1c,0x29,0xf1,0xe0,0xd7,0x1f,0x18,0x2a,0xd7,0x25,0x26,0x2b,0xd7,0x19,0x1c,0x1c,0x25,0xd7,0x20,0x24,0x27,0x23,0x1c,0x24,0x1c,0x25,0x2b,0x1c,0x1b]

				fileprivate func targetBackground(cell num: UInt8) -> UInt8 {
					let value = Int(num) - 183
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "F5F5F5" :*/
fileprivate let constToModelPath:String = "indicatorindicatorindicator"

/*: "#128CFF" :*/
fileprivate let main_makeTitle:String = "frame scale as#128CF"
fileprivate let constActualValue:[Character] = ["F"]

/*: "Feedback" :*/
fileprivate let showPointTitle:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let user_dropData:String = "#"
fileprivate let app_shareInsertSendValue:String = "title m guard8370FF"

/*: "Quick Greeting" :*/
fileprivate let kAwakeKey:String = "ready userQuick "
fileprivate let user_viewSuiteUrl:[Character] = ["G"]
fileprivate let dataMakeMessage:[Character] = ["r","e","e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let notiUserId:[Character] = ["B","e","a","u","t"]
fileprivate let constPermissionKey:String = "y Photosto var video"

/*: "unlockGift" :*/
fileprivate let app_layerMessage:String = "utransformock"

/*: "isBurn" :*/
fileprivate let constGiftData:[UInt8] = [0xde,0xe8,0xb7,0xea,0xe7,0xe3]

				fileprivate func sizeName(share num: UInt8) -> UInt8 {
					let value = Int(num) + 139
					if value > 255 {
						return UInt8(value - 256)
					} else {
						return UInt8(value)
					}
				}

/*: "giftId" :*/
fileprivate let dataPushTextName:[Character] = ["g","i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let const_serverKey:[Character] = ["r","Y"]
fileprivate let const_detailText:String = "view let descriptionMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let user_moreId:String = "GJ:Callvalue make height"
fileprivate let noti_finishValue:String = "user centerCustom"

/*: "changeDirection" :*/
fileprivate let appMeStr:String = "normalhang"
fileprivate let const_modeTitle:String = "ctmaken"

/*: "msgInfo" :*/
fileprivate let userUpTitleId:String = "msgInsituation app case"
fileprivate let k_emptyName:String = "view"

/*: "wantGift" :*/
fileprivate let noti_aftTitle:String = "wantGibirthday index if let icon"
fileprivate let k_originContent:String = "fcolor"

/*: "jumpKey" :*/
fileprivate let constAddDeleteFormat:String = "jumpKself table type as make"
fileprivate let notiArrayPath:String = "ewindow"

/*: "truePersonAuth" :*/
fileprivate let mainUserData:String = "truePmake cell"
fileprivate let const_titleName:String = "path type play fileersonAuth"

/*: "logType" :*/
fileprivate let show_miniPath:String = "connect returnlogTy"
fileprivate let showMaximumData:String = "usere"

/*: "msgType" :*/
fileprivate let main_withPlayerUrl:[UInt8] = [0x29,0x37,0x23,0x10,0x3d,0x34,0x21]

/*: "QuickMsg" :*/
fileprivate let notiManagerName:String = "self type selfQui"
fileprivate let userErrorText:[Character] = ["c","k","M","s","g"]

/*: "params" :*/
fileprivate let data_viewMessage:[UInt8] = [0x50,0x41,0x52,0x41,0x4d,0x53]

				fileprivate func addWait(max num: UInt8) -> UInt8 {
					let value = Int(num) - 224
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "content" :*/
fileprivate let appStyleTitleNameMessage:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "message" :*/
fileprivate let show_insertTitle:[UInt8] = [0x61,0x69,0x7f,0x7f,0x6d,0x6b,0x69]

				private func pathApp(request num: UInt8) -> UInt8 {
					return num ^ 12
				}

/*: "TxtMsg" :*/
fileprivate let showEqualPath:String = "TxtMstype string"
fileprivate let notiButtonFormat:[Character] = ["g"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let show_colorFormat:[UInt8] = [0xf5,0xcc,0xca,0xc0,0xc6,0x83,0xcb,0xc6,0xcf,0xcf,0xcc,0x83,0xc7,0xcc,0xd4,0xcd,0xcf,0xcc,0xc2,0xc7,0x83,0xc5,0xc2,0xca,0xcf,0xc6,0xc7,0x8d,0x83,0xf3,0xd1,0xcc,0xc0,0xc6,0xc7,0xd6,0xd1,0xc6]

/*: "length" :*/
fileprivate let user_sizeTitleMsg:String = "toength"

/*: "audioId" :*/
fileprivate let userIconData:[Character] = ["a"]
fileprivate let noti_liveContent:String = "udioIdshared else succeed voice self"

/*: "audioLength" :*/
fileprivate let notiChangeKey:[UInt8] = [0x1b,0x2f,0x1e,0x23,0x29,0x6,0x1f,0x28,0x21,0x2e,0x22]

				fileprivate func dataToUser(aft num: UInt8) -> UInt8 {
					let value = Int(num) + 70
					if value > 255 {
						return UInt8(value - 256)
					} else {
						return UInt8(value)
					}
				}

/*: "audioSandbox" :*/
fileprivate let k_sizeFlushId:String = "AUDIO"
fileprivate let noti_fatalName:[Character] = ["S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let notiTapKey:[UInt8] = [0x24,0x32,0x39,0x33,0x32,0x25,0x1e,0x33]

				private func effectViewBag(say num: UInt8) -> UInt8 {
					return num ^ 87
				}

/*: "AudioMsg" :*/
fileprivate let show_fillMsg:[Character] = ["A","u","d","i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let constBackTitle:[Character] = ["C","a","n","\'","t"," ","s"]
fileprivate let user_viewTitle:[Character] = ["e","n","d"," "]
fileprivate let constModelUrl:[Character] = ["b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let mainOfStr:String = "model let contentquoteM"

/*: "quoteMsgInfo" :*/
fileprivate let kEffectName:[Character] = ["q","u","o","t","e","M","s","g","I"]
fileprivate let const_profileValue:String = "lasto"

/*: "contentType" :*/
fileprivate let k_rootFormat:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let main_rangeValue:String = "ImgMsgtype request user"

/*: "imageUri" :*/
fileprivate let mainZoneMaxFormat:[UInt8] = [0x93,0x97,0x9b,0x9d,0x9f,0xaf,0x88,0x93]

				private func arrayCurrent(top num: UInt8) -> UInt8 {
					return num ^ 250
				}

/*: "customSend" :*/
fileprivate let notiQuantityStr:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "VideoMsg" :*/
fileprivate let mainTotalId:String = "let make viewVideoM"
fileprivate let user_sharedName:[Character] = ["s","g"]

/*: "coverImg" :*/
fileprivate let kColorFormat:[UInt8] = [0x60,0x6c,0x75,0x66,0x71,0x4a,0x6e,0x64]

				private func nationalService(range num: UInt8) -> UInt8 {
					return num ^ 3
				}

/*: "videoUrl" :*/
fileprivate let showTitlePath:[UInt8] = [0xe,0x11,0x1c,0x1d,0x17,0x2d,0xa,0x14]

				private func userSource(say num: UInt8) -> UInt8 {
					return num ^ 120
				}

/*: "duration" :*/
fileprivate let app_currentStr:[UInt8] = [0x7c,0x6d,0x6a,0x79,0x6c,0x71,0x77,0x76]

				private func theLikesOf(bag num: UInt8) -> UInt8 {
					return num ^ 24
				}

/*: "Documents/User/voice/" :*/
fileprivate let showTimeOutputFormat:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r"]
fileprivate let user_statusMessage:[Character] = ["/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let main_shareValue:String = "Documline name model case image"
fileprivate let show_viewMomentFormat:String = "/User/relse error content"
fileprivate let userMainName:String = "page reason selfecord/"

/*: "msgIncome" :*/
fileprivate let kSuiteToUrl:String = "alongside color addmsgInco"
fileprivate let app_viewTitle:[Character] = ["m","e"]

/*: "mfBean" :*/
fileprivate let kUserPath:[UInt8] = [0xf6,0xfd,0xd9,0xfe,0xfa,0xf5]

				private func colorCell(shared num: UInt8) -> UInt8 {
					return num ^ 155
				}

/*: "messageType" :*/
fileprivate let k_mainMessage:[Character] = ["m","e","s","s","a","g","e","T","y"]
fileprivate let constMakeImageFormat:String = "pconversation"

/*: "WantGift" :*/
fileprivate let noti_showValue:String = "Wantgift share lab"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let userPicStr:String = "_Priseat cling count show count"
fileprivate let notiRangeFromData:String = "interaction"
fileprivate let appFileEventMessage:String = "value open self takepsTitle_"

/*: "totalMfCoin" :*/
fileprivate let k_videoUrl:[Character] = ["t","o","t","a","l","M","f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let kScalePath:String = "attachSmodel self reply as"
fileprivate let userLeadingDayStr:String = "view selfysMsg"

/*: "intimatePhoto" :*/
fileprivate let const_labelObjectData:String = "insaymate"
fileprivate let app_increaseMsg:[Character] = ["P","h","o","t","o"]

/*: "totalIntimate" :*/
fileprivate let user_countTitle:String = "tbeautyal"

/*: "intimate" :*/
fileprivate let show_videoStr:[UInt8] = [0xb3,0xb4,0xae,0xb3,0xb7,0xbb,0xae,0xbf]

				private func nativeManager(error num: UInt8) -> UInt8 {
					return num ^ 218
				}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let dataModelValue:[UInt8] = [0x91,0xb3,0xbc,0xf5,0xa6,0xf2,0xa7,0xa1,0xb7,0xf2,0xa6,0xba,0xb7,0xf2,0xb4,0xa7,0xbc,0xb1,0xa6,0xbb,0xbd,0xbc,0xf2,0xb6,0xa7,0xb7,0xf2,0xa6,0xbd,0xf2,0xab,0xbd,0xa7,0xa0,0xf2,0xbe,0xb3,0xb1,0xb9,0xf2,0xbd,0xb4,0xf2,0xbb,0xbc,0xa6,0xbb,0xbf,0xb3,0xb1,0xab,0xf2,0xbe,0xb7,0xa4,0xb7,0xbe,0xfc]

				private func tapBottomGift(make num: UInt8) -> UInt8 {
					return num ^ 210
				}

/*: "giftNum" :*/
fileprivate let appPathKey:String = "giftNumcenter return"

/*: "pkgItemsetId" :*/
fileprivate let showAppMessage:String = "photo i labpkgI"
fileprivate let show_giftTitle:String = "etIdinfo raw to"

/*: "wantGiftId" :*/
fileprivate let user_restoreMakeData:String = "count base viewwantG"

/*: "WantGiftMsg" :*/
fileprivate let constNeedName:String = "Wantplayer extension self"
fileprivate let showContextKey:[Character] = ["s","g"]

/*: "View info" :*/
fileprivate let user_makeMsg:String = "Viclass model self make string"
fileprivate let appInputTitle:String = "ew infoif log false"

/*: "Report" :*/
fileprivate let const_snapId:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let userClickValue:String = "control title min typeView"
fileprivate let const_managerMessage:String = "text capture head colord recei"
fileprivate let constToTitle:[Character] = ["p","t","s","?"]

/*: "%@ is using too low a version" :*/
fileprivate let show_timeViewOfId:String = "main color share view return%@ i"
fileprivate let userSendId:String = "ing toplayer model mixture head else"
fileprivate let appWithMakeTotalKey:[Character] = ["a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let dataObserverMessage:String = "Shoot at screen"
fileprivate let mainLabelId:[Character] = ["l","e","a","s","t"," "]
fileprivate let dataEditMessage:String = "5 secondsto size app title"

/*: "Upload failed" :*/
fileprivate let show_modelPath:[Character] = ["U","p","l","o","a","d"," ","f","a","i","l"]
fileprivate let noti_colorPassData:[Character] = ["e","d"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let k_methodMessage:[UInt8] = [0xc0,0xd6,0xdc,0x87,0xca,0xc8,0xd5,0x87,0xdc,0xda,0xcc,0x87,0xdb,0xcf,0xcc,0x87,0xdd,0xd0,0xcb,0xcc,0xd6,0x87,0xca,0xcf,0xc8,0xdb,0x87,0xcd,0xcc,0xc8,0xdb,0xdc,0xd9,0xcc,0x87,0xdb,0xd6,0x87,0xce,0xd0,0xdd,0xcc,0x87,0x8c,0xa7,0x87,0xc8,0x87,0xca,0xc8,0xd3,0xd3]

				fileprivate func mediumEqual(background num: UInt8) -> UInt8 {
					let value = Int(num) - 103
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "cellData" :*/
fileprivate let appImageData:[Character] = ["c","e","l","l","D","a","t"]
fileprivate let app_fileData:String = "same"

/*: "System notification" :*/
fileprivate let appKitName:String = "var list icon loadSystem "
fileprivate let mainLoadValue:[Character] = ["n","o","t"]
fileprivate let userWhiteName:String = "IFIC"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InfoThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum TitlePicFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class InfoThen: ReloadViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = BivouacCacheModel()
    private var userWrap = BivouacCacheModel() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = TitlePicFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: BivouacCacheModel = BivouacCacheModel()) {
    init(chatID: String, isFrom: TitlePicFromEnum = .Normal, userWrap: BivouacCacheModel = BivouacCacheModel()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        InfoThen.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        WithCenterThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == PicMacroDefine.getXiaoMiID() {
        if targetId == PicMacroDefine.tapId() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == PicMacroDefine.getCustomerServiceID() {
        } else if targetId == PicMacroDefine.harvestMoon() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_equalUrl.map{targetBackground(cell: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ExamineedReactiveCompatible.share.appConfigMode.disableShootScreen {
            if ExamineedReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = DramaticCompositionShieldView(frame: CGRect(x: 0, y: 0, width: user_barText, height: show_licenseData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        clearAway()
        //: createUI()
        anyMakeUi()
        //: createUIConstraints()
        instanceAcrossConstraints()
        //: bindInteraction()
        dataTo()
        //: addTapGestureRecognizer()
        wait()
        //: func__requestChatInfo()
        past()
        //: func__getFillDraftText()
        phoneSystem()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        ServerManagerDelegate.shared.glasses()
        //: func__addVoiceNotification()
        blockBackground()
        //: addReadPeceiptNotification()
        objectAdd()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            keyPath()
            //: func__TipsNotification()
            keepAnEyeOn()
            //: if ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.male.rawValue {
                //: func__setReplyTipsView()
                draftCopy()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        rrmove(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        rrmove(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            pointOut()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            MessageRatingManager.shared.checked()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: MessageViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MessageViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (constToModelPath.replacingOccurrences(of: "indicator", with: "F5")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(CellInfoReactiveCompatible.self, forCellReuseIdentifier: CellInfoReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ResponseViewCell.self, forCellReuseIdentifier: ResponseViewCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(MessageMsgCell.self, forCellReuseIdentifier: MessageMsgCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(FinancialAidReactiveCompatible.self, forCellReuseIdentifier: FinancialAidReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(DorsalVertebraThen.self, forCellReuseIdentifier: DorsalVertebraThen.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(SkipOverMessageCell.self, forCellReuseIdentifier: SkipOverMessageCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(TableMsgCell.self, forCellReuseIdentifier: TableMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(EasingMsgCell.self, forCellReuseIdentifier: EasingMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(NameMsgCell.self, forCellReuseIdentifier: NameMsgCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: WireReactiveCompatible.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(LightViewCell.self, forCellReuseIdentifier: WireReactiveCompatible.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: RubricErrorDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = RubricErrorDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.listener()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: EnrichInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: DismissSignalingViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = EnrichInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(main_makeTitle.suffix(6)) + String(constActualValue)))!, .font: UIFont.font(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(showPointTitle)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(greetChange), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: AggregationThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = AggregationThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: TableMessageTitleView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = TableMessageTitleView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.font(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (user_dropData.capitalized + String(app_shareInsertSendValue.suffix(6)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(kAwakeKey.suffix(6)) + String(user_viewSuiteUrl) + String(dataMakeMessage)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.titleDevice().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(removeFile), for: .touchUpInside)
        //: btn.isHidden = !ExamineedReactiveCompatible.share.appUserConfigMode.showDirectGreet || ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || ExamineedReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !ExamineedReactiveCompatible.share.appUserConfigMode.showDirectGreet || ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.male.rawValue || ExamineedReactiveCompatible.share.appStatus == InstanceHashableRepresentation.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: PicTipsReactiveCompatible? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = PicTipsReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: ValueTranslationView = {
        //: let v = TalkingSelectTranslationView()
        let v = ValueTranslationView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.menu(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: ListMessageView = {
        //: let v = TalkingTranslationResultView()
        let v = ListMessageView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.nameAppearModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.font(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (user_dropData.capitalized + String(app_shareInsertSendValue.suffix(6)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(notiUserId) + String(constPermissionKey.prefix(8))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.titleDevice().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fromOf), for: .touchUpInside)
        //: btn.isHidden = ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || ExamineedReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.male.rawValue || ExamineedReactiveCompatible.share.appStatus == InstanceHashableRepresentation.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: SelectedViewDelegate = {
        //: let v = TalkingLiveRoomGamesView()
        let v = SelectedViewDelegate()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: EqualSizeThen = {
        //: let view = TalkingPrivateBannerView.init()
        let view = EqualSizeThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: AddScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = AddScreenView()
        //: v.isHidden = (ExamineedReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (ExamineedReactiveCompatible.share.appStatus == InstanceHashableRepresentation.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: RefView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = RefView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.arrayColor()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension InfoThen: DisappearFileThen {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func banner(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.beyondShared(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension InfoThen: MessageIndexHandle {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func removeFile() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        WithCenterThen.sectionPlay(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.heatherMixtureMoment(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.positionExecuteView(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func fromOf() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        WithCenterThen.enableBusinessAsHandler(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = PhotoRestoreReactiveCompatible()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().applicatorEqual(urlStr: dict?["url"] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(app_layerMessage.replacingOccurrences(of: "transform", with: "nl") + "Gift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = AspectModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: constGiftData.map{sizeName(share: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(dataPushTextName))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.scoff(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> SideCellData! {
    func content(_: MessageViewController!, viewInfo data: V2TIMMessage!) -> SideCellData! {
        //: let dic = PublisherThen.parseTXMessageData(data: data.customElem.data)
        let dic = PublisherThen.parse(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TitleChatManager.cornerMetadata(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: PaperThen.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(const_serverKey) + String(const_detailText.suffix(7)))].stringValue == (String(user_moreId.prefix(7)) + String(noti_finishValue.suffix(6))) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(appMeStr.replacingOccurrences(of: "normal", with: "c") + "eDire" + const_modeTitle.replacingOccurrences(of: "make", with: "io"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic["extra"][(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo"))]["toUid"].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = LoadCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = LoadCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: DismissInstanceCellData.self) == true ||
        if cellData?.isKind(of: DismissInstanceCellData.self) == true ||
            //: cellData?.isKind(of: CompartmentCellData.self) == true ||
            cellData?.isKind(of: CompartmentCellData.self) == true ||
            //: cellData?.isKind(of: LoadCellData.self) == true ||
            cellData?.isKind(of: LoadCellData.self) == true ||
            //: cellData?.isKind(of: StreetSmartCellData.self) == true {
            cellData?.isKind(of: StreetSmartCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            WithCenterThen.isochronousColor(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            WithCenterThen.requestPic(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: SideCellData!) -> WireReactiveCompatible! {
    func impactPhoto(_: MessageViewController!, camera data: SideCellData!) -> WireReactiveCompatible! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = walkToModel(data: data)

        //: if data.isKind(of: LoadCellData.self) {
        if data.isKind(of: LoadCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = CellInfoReactiveCompatible(style: .default, reuseIdentifier: CellInfoReactiveCompatible.className())
            //: let cellData = data as! LoadCellData
            let cellData = data as! LoadCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.tvMsg() {
                //: if cellData.msgModel.quoteMsgInfo.uid == ExamineedReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == ExamineedReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = ExamineedReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = ExamineedReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: CompartmentCellData.self) {
        if data.isKind(of: CompartmentCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ResponseViewCell(style: .default, reuseIdentifier: ResponseViewCell.className())
            //: let cellData = data as! CompartmentCellData
            let cellData = data as! CompartmentCellData
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: StreetSmartCellData.self) {
        if data.isKind(of: StreetSmartCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = MessageMsgCell(style: .default, reuseIdentifier: MessageMsgCell.className())
            //: let cellData = data as! StreetSmartCellData
            let cellData = data as! StreetSmartCellData
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: PlayerCellData.self) {
        if data.isKind(of: PlayerCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = FinancialAidReactiveCompatible(style: .default, reuseIdentifier: FinancialAidReactiveCompatible.className())
            //: let cellData = data as! PlayerCellData
            let cellData = data as! PlayerCellData
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: CutCellData.self) {
        if data.isKind(of: CutCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = DorsalVertebraThen(style: .default, reuseIdentifier: DorsalVertebraThen.className())
            //: let cellData = data as! CutCellData
            let cellData = data as! CutCellData
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = HeadTransformable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.unboundTiffin(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(noti_aftTitle.prefix(6)) + k_originContent.replacingOccurrences(of: "color", with: "t")))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: DismissInstanceCellData.self) {
        if data.isKind(of: DismissInstanceCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = EasingMsgCell(style: .default, reuseIdentifier: EasingMsgCell.className())
            //: let cellData = data as! DismissInstanceCellData
            let cellData = data as! DismissInstanceCellData
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: PaperThen.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = TableMsgCell(style: .default, reuseIdentifier: TableMsgCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! PaperThen
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: ReloadReactiveCompatible.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = SkipOverMessageCell(style: .default, reuseIdentifier: SkipOverMessageCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! ReloadReactiveCompatible
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: NewspaperPublisherCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = NameMsgCell(style: .default, reuseIdentifier: NameMsgCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! NewspaperPublisherCellData
            //: cell.fill(with: cellData)
            cell.create(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: WireReactiveCompatible!) {
    func reply(_: MessageViewController!, messageAvatar cell: WireReactiveCompatible!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != PicMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != PicMacroDefine.harvestMoon() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: AppPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            AppPushManager.share.enrich(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: WireReactiveCompatible!) {
    func sinceMin(_: MessageViewController!, view cell: WireReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ResponseViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! CompartmentCellData
            let imageCellData = cell.messageData as! CompartmentCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = TitleThen(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.timeStatus(imageCellData, context: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.turn(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_screenData)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.numberView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.titleDict(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                sharedAwake()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(notiDeviceName)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = SizePhotoModel()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = LanthanonMeasurable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [SizePhotoModel] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = PublisherDataSource(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                sharedAwake()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: MessageMsgCell.self) { // 视频
            //: let videoCellData = cell.messageData as! StreetSmartCellData
            let videoCellData = cell.messageData as! StreetSmartCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = ValueTableRecognizerDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                sharedAwake()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = AdultPreviewVc(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.contenture(videoCellData, telecasting: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.turn(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_screenData)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.numberView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.titleDict(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                sharedAwake()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: EasingMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! EasingMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! DismissInstanceCellData, cellView: audioCell)
            phonationUser(cellData: audioCell.cellData as! DismissInstanceCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: CellInfoReactiveCompatible.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! CellInfoReactiveCompatible
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = PublisherThen.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = PublisherThen.parse(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic["extra"] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(constAddDeleteFormat.prefix(5)) + notiArrayPath.replacingOccurrences(of: "window", with: "y"))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(constAddDeleteFormat.prefix(5)) + notiArrayPath.replacingOccurrences(of: "window", with: "y"))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(mainUserData.prefix(5)) + String(const_titleName.suffix(9))) {
                    //: AppPushManager.share.func__pushUserVerifyController(toast: nil)
                    AppPushManager.share.addToast(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<WorldViewHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: AppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    AppPushManager.share.belowTheoreticalAccount(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(const_serverKey) + String(const_detailText.suffix(7)))] as? String == (String(user_moreId.prefix(7)) + String(noti_finishValue.suffix(6))) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo"))][(String(show_miniPath.suffix(5)) + showMaximumData.replacingOccurrences(of: "user", with: "p"))].intValue == 1 {
                    //: checkAndCallVoice()
                    preserve()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    languageUnit()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: SkipOverMessageCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! SkipOverMessageCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = PublisherThen.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = PublisherThen.parse(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic["extra"] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(constAddDeleteFormat.prefix(5)) + notiArrayPath.replacingOccurrences(of: "window", with: "y"))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<WorldViewHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: AppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    AppPushManager.share.belowTheoreticalAccount(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: TableMsgCell.self) {
            //: AppPushManager.share.func__pushToUserDetailVC(uid: targetId)
            AppPushManager.share.enrich(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: WireReactiveCompatible!) {
    func bottom(_: MessageViewController!, send cell: WireReactiveCompatible!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: PointReactiveCompatible?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ResponseViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ResponseViewCell
            //: cellData = imageCell.cellData as! CompartmentCellData
            cellData = imageCell.cellData as! CompartmentCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: MessageMsgCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! MessageMsgCell
            //: cellData = videoCell.cellData as! StreetSmartCellData
            cellData = videoCell.cellData as! StreetSmartCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: EasingMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! EasingMsgCell
            //: cellData = audioCell.cellData as! DismissInstanceCellData
            cellData = audioCell.cellData as! DismissInstanceCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: CellInfoReactiveCompatible.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! CellInfoReactiveCompatible
            //: cellData = textCell.cellData as! LoadCellData
            cellData = textCell.cellData as! LoadCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: SideCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: SideCellData? = TitleChatManager.playerData(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: StreetSmartCellData.self) {
                if reCellData!.isKind(of: StreetSmartCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    conversationModel(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    lookSumo(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: SideCellData!) {
    func context(_: MessageViewController!, broadcastUniform cellData: SideCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = WithCenterThen.untilTitle(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: SideCellData!) {
    func render(_: MessageViewController!, info cellData: SideCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        WithCenterThen.hidden(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: SideCellData) -> SideCellData {
    private func walkToModel(data: SideCellData) -> SideCellData {
        //: let isSelf = data.msgModel.user.uid == Int(ExamineedReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(ExamineedReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if ExamineedReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if ExamineedReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = ExamineedReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = ExamineedReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = ExamineedReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = ExamineedReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func sizeUpTitle() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: "txt"]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = ["type": (String(notiManagerName.suffix(3)) + String(userErrorText))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo, String(bytes: data_viewMessage.map{addWait(max: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TitleChatManager.messageInfo(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.lookSumo(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func positionExecuteView(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: appStyleTitleNameMessage.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: appStyleTitleNameMessage.reversed(), encoding: .utf8)!: content, "txtId": greetId, String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: "txt"]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: show_insertTitle.map{pathApp(request: $0)}, encoding: .utf8)!: content, "txtId": greetId, "type": (String(showEqualPath.prefix(5)) + String(notiButtonFormat))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo, String(bytes: data_viewMessage.map{addWait(max: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = TitleChatManager.messageInfo(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.lookSumo(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            ScutWorkReactiveCompatible.shared.sendHandle(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.heatherMixtureMoment(showMsg: String(bytes: show_colorFormat.map{$0^163}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        WithCenterThen.dataQuery(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(user_sizeTitleMsg.replacingOccurrences(of: "to", with: "l"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: "audio",
                               //: "audioId": greetId,
                               (String(userIconData) + String(noti_liveContent.prefix(6))): greetId,
                               //: "audioLength": length,
                               String(bytes: notiChangeKey.map{dataToUser(aft: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (k_sizeFlushId.lowercased() + String(noti_fatalName)): model.unZipFilePath,
                               //: "senderId": ExamineedReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: notiTapKey.map{effectViewBag(say: $0)}, encoding: .utf8)!: ExamineedReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(userIconData) + String(noti_liveContent.prefix(6))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: notiChangeKey.map{dataToUser(aft: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (k_sizeFlushId.lowercased() + String(noti_fatalName)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 "type": (String(show_fillMsg))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo, String(bytes: data_viewMessage.map{addWait(max: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = TitleChatManager.messageInfo(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.lookSumo(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func nameAppearModel(msgStr: String, quoteModel: CancelModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            heatherMixtureMoment(showMsg: (String(constBackTitle) + String(user_viewTitle) + String(constModelUrl)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: appStyleTitleNameMessage.reversed(), encoding: .utf8)!: temStr, String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: "txt"]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: show_insertTitle.map{pathApp(request: $0)}, encoding: .utf8)!: temStr, "type": (String(showEqualPath.prefix(5)) + String(notiButtonFormat))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(mainOfStr.suffix(6)) + "sgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(kEffectName) + const_profileValue.replacingOccurrences(of: "last", with: "nf"))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: data_viewMessage.map{addWait(max: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TitleChatManager.messageInfo(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? CancelModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            lookSumo(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func scoff(img: UIImage, sendModel: AspectModel) {
        //: let imagePath = CompartmentCellData.getChatImagePath(image: img)
        let imagePath = CompartmentCellData.aboveStatus(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: k_rootFormat.reversed(), encoding: .utf8)!: (String(main_rangeValue.prefix(6))), String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: "img", String(bytes: mainZoneMaxFormat.map{arrayCurrent(top: $0)}, encoding: .utf8)!: imagePath, String(bytes: notiQuantityStr.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = ["pic": imagePath,
                                        //: "type": "ImgMsg",
                                        "type": (String(main_rangeValue.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: constGiftData.map{sizeName(share: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(dataPushTextName)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo, String(bytes: data_viewMessage.map{addWait(max: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TitleChatManager.messageInfo(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            lookSumo(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: SideCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func conversationModel(msgData: SideCellData?, sendModel: AspectModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: k_rootFormat.reversed(), encoding: .utf8)!: (String(mainTotalId.suffix(6)) + String(user_sharedName)), String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: "video", String(bytes: notiQuantityStr.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = ["type": (String(mainTotalId.suffix(6)) + String(user_sharedName)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: kColorFormat.map{nationalService(range: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: showTitlePath.map{userSource(say: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: app_currentStr.map{theLikesOf(bag: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: constGiftData.map{sizeName(share: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(dataPushTextName)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo, String(bytes: data_viewMessage.map{addWait(max: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = TitleChatManager.messageInfo(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.send(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.locationLive(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.no(cellData, nomDeGuerre: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.locationLive(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.germanism(dict, changeSize: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        lookSumo(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func legalStatus(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: "audio",
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: notiChangeKey.map{dataToUser(aft: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (k_sizeFlushId.lowercased() + String(noti_fatalName)): voicePath,
                       //: "senderId": ExamineedReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: notiTapKey.map{effectViewBag(say: $0)}, encoding: .utf8)!: ExamineedReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(k_sizeFlushId.lowercased() + String(noti_fatalName)): voicePath,
                         //: "type": "AudioMsg",
                         "type": (String(show_fillMsg)),
                         //: "len": "\(voiceTime)"]
                         "len": "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo, String(bytes: data_viewMessage.map{addWait(max: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TitleChatManager.messageInfo(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            lookSumo(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: SideCellData, reSend: Bool) {
    private func lookSumo(msgData: SideCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            heatherMixtureMoment(showMsg: data_keyFormat)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params["toUid"] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: StreetSmartCellData.self) == false && msgData.isKind(of: CutCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: StreetSmartCellData.self) == false, msgData.isKind(of: CutCellData.self) == false, params["type"] as? String != (String(notiManagerName.suffix(3)) + String(userErrorText)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.send(msgData)
        }
        //: let recordTime = PublisherThen.getMessageInsertTime()
        let recordTime = PublisherThen.voiceColor()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains("pic") { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params["pic"] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params["pic"] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params["type"] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(show_fillMsg)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((k_sizeFlushId.lowercased() + String(noti_fatalName))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(k_sizeFlushId.lowercased() + String(noti_fatalName))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (k_sizeFlushId.lowercased() + String(noti_fatalName)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(showTimeOutputFormat) + String(user_statusMessage))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = main_replaceMsg + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(main_shareValue.prefix(5)) + "ents" + String(show_viewMomentFormat.prefix(7)) + String(userMainName.suffix(6)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = noti_numberValue + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(userIconData) + String(noti_liveContent.prefix(6)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params["audio"] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: SizeRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        SizeRequestTool.giveaway(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(notiManagerName.suffix(3)) + String(userErrorText)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.leftHandednessAlways(msgData, add: nil, pathFor: NSNumber(value: 0), statusProceedsChangeUse: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.turn(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: app_buttonMessage)
                //: if params.keys.contains("txtId") {
                if params.keys.contains("txtId") {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    WithCenterThen.dataQuery(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.enrichment(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo"))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(kSuiteToUrl.suffix(7)) + String(app_viewTitle))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(kSuiteToUrl.suffix(7)) + String(app_viewTitle))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if LiveManager.upShared().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: appPackageText, object: nil, userInfo: [String(bytes: kUserPath.map{colorCell(shared: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.radiogramState(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(k_mainMessage) + constMakeImageFormat.replacingOccurrences(of: "conversation", with: "e"))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(notiManagerName.suffix(3)) + String(userErrorText)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!] = (String(noti_showValue.prefix(4)) + "Gift")
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    main_inviteMsg.smartViewButton(eventID: appSystemValue)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(notiManagerName.suffix(3)) + String(userErrorText)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!] = "txt"
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = TitleChatManager.messageInfo(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.send(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(ExamineedReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(ExamineedReactiveCompatible.share.loginUserMode.userID)" + (String(userPicStr.prefix(4)) + "vateChatT" + notiRangeFromData.replacingOccurrences(of: "interaction", with: "i") + String(appFileEventMessage.suffix(8))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.male.rawValue, type != (String(notiManagerName.suffix(3)) + String(userErrorText)) {
                //: self.removeReplyTipsView()
                self.atFor()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.leftHandednessAlways(updateMsgData, add: msgInfo["msgId"] as? String, pathFor: msgIncome, statusProceedsChangeUse: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            WithCenterThen.requestPic(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(k_videoUrl))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(k_videoUrl))] as! NSNumber
                //: ExamineedReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                ExamineedReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(kScalePath.prefix(7)) + String(userLeadingDayStr.suffix(5)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(kScalePath.prefix(7)) + String(userLeadingDayStr.suffix(5)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.observerTo(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((const_labelObjectData.replacingOccurrences(of: "say", with: "ti") + String(app_increaseMsg))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(const_labelObjectData.replacingOccurrences(of: "say", with: "ti") + String(app_increaseMsg))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? RequestPermissionModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.timeStatus(updateMsgData, context: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo["video"] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? ClearSendModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.contenture(updateMsgData, telecasting: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        towardFirst()

        //: updateSession()
        restoreOf()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func restoreOf() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        ViewValueInfoManager.specialSessionWith(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func turn(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard ExamineedReactiveCompatible.share.loginUserMode.status != 1 else {
            guard ExamineedReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    heatherMixtureMoment(showMsg: errorStr)
                }
                //: return
                return
            }

            //: AppPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            AppPushManager.share.betweenArray(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.deadlineCurrency()
            //: ExamineedReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            ExamineedReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.listener()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = CenterSpendView(frame: CGRect(x: 0, y: 0, width: user_barText, height: show_licenseData))
            //: view.show()
            view.equalIn()
            //: } else if errorCode == 60003, !ExamineedReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !ExamineedReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            SizeSearchWindowManager.shared.straitAndNarrow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                heatherMixtureMoment(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func titleDict(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo"))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.radiogramState(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func statusFreshman() {
        //: if self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.selectBy(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(ExamineedReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(ExamineedReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (const_appStr.array(forKey: dataPostMsg)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.gestureAnimation()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                const_appStr.set(intimateCacheData, forKey: dataPostMsg)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func deadlineNotification(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = PublisherThen.parseTXMessageData(data: data.customElem.data)
        let dic = PublisherThen.parse(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic["extra"].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo"))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(user_countTitle.replacingOccurrences(of: "beauty", with: "ot") + "Intimate")]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.timeInsert(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func radiogramState(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((user_countTitle.replacingOccurrences(of: "beauty", with: "ot") + "Intimate")), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(user_countTitle.replacingOccurrences(of: "beauty", with: "ot") + "Intimate")] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.timeInsert(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func timeInsert(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = ViewValueInfoManager.cache(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.selectBy(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.animSumimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.exhibitTop(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.exhibitTop(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        ViewValueInfoManager.submit(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = ["uid": self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: show_videoStr.map{nativeManager(error: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: appDeviceButtonValue,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension InfoThen: RequestReactiveCompatible {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func equalSecond() {
        //: guard ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.viewSignal(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func headRange(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func save(actionType: DataScalar) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    take()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    underExhibit()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                take()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if ExamineedReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if ExamineedReactiveCompatible.share.loginUserMode.sex == CommentRefCompatibleValue.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if ExamineedReactiveCompatible.share.loginUserMode.loungePlus {
                if ExamineedReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.toOmit(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                underExhibit()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    visualCommunication()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    underExhibit()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                visualCommunication()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.imageFrom(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func underExhibit() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.heatherMixtureMoment(showMsg: String(bytes: dataModelValue.map{tapBottomGift(make: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func nameTheory(giftModel _: HeadTransformable, giftNum _: String, model _: VoiceMeasurable) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func popObserver(giftModel: HeadTransformable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        unboundTiffin(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: CutCellData?, fromStr: String?) {
    func unboundTiffin(giftModel: HeadTransformable, giftNum: String, cellData: CutCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            heatherMixtureMoment(showMsg: data_keyFormat)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params["toUid"] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(dataPushTextName))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(appPathKey.prefix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(showAppMessage.suffix(4)) + "tems" + String(show_giftTitle.prefix(4)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params["from"] = fromStr
        }
        // 送礼接口
        //: SizeRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        SizeRequestTool.dismissUser(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.turn(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kScreenName)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.sexAction(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.numberView(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.restoreOf()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo"))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((user_countTitle.replacingOccurrences(of: "beauty", with: "ot") + "Intimate")), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(user_countTitle.replacingOccurrences(of: "beauty", with: "ot") + "Intimate")] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.timeInsert(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.giftCenter(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                main_inviteMsg.smartViewButton(eventID: noti_environmentTitle)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func transmit(giftModel: HeadTransformable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        main_inviteMsg.smartViewButton(eventID: appMessageId)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            heatherMixtureMoment(showMsg: data_keyFormat)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= ExamineedReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            underExhibit()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(user_restoreMakeData.suffix(5)) + "iftId")] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params["type"] = (String(constNeedName.prefix(4)) + "GiftM" + String(showContextKey))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: (String(noti_showValue.prefix(4)) + "Gift")]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo")): msgInfo, String(bytes: data_viewMessage.map{addWait(max: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TitleChatManager.messageInfo(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            lookSumo(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func toButton(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kRecordFormat - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(true)
            self.msgView.scroll(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func loftiness(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(true)
            self.msgView.scroll(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func makeRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.makeRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                withDeviceRequest(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func itemPost() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if ServerManagerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            ServerManagerDelegate.shared.stopShow()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func numberView(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo"))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!] = "gift"
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(userUpTitleId.prefix(5)) + k_emptyName.replacingOccurrences(of: "view", with: "fo"))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TitleChatManager.messageInfo(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.send(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kSuiteToUrl.suffix(7)) + String(app_viewTitle))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kSuiteToUrl.suffix(7)) + String(app_viewTitle))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.leftHandednessAlways(cellData, add: msgInfoDic["msgId"] as? String, pathFor: msgIncome, statusProceedsChangeUse: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        listDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(k_videoUrl))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(k_videoUrl))] as! NSNumber
            //: ExamineedReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            ExamineedReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: ExamineedReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.telephoneMessage(needReload: false, mf_coin: ExamineedReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func keyPath() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.addShow(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func listDic(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.shadowMake(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func page(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.noToArr(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func withList() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func preserve() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.videoComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.restoreOf()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func languageUnit() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.sex { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.restoreOf()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension InfoThen {
    //: @objc private func feedbackBtnClick() {
    @objc private func greetChange() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = DisappearViewDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        sharedAwake()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func nameOf() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = WaitDataSource(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.zapTime(cellTitleList: [(String(user_makeMsg.prefix(2)) + String(appInputTitle.prefix(7))).localized, "Notes".localized, (String(const_snapId)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .equalTarget()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: AppPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                AppPushManager.share.enrich(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                arrayColor()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = StraddleAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.control(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func arrayColor() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = SectionThen()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.night(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.instanceIn(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func finishClick() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            withDeviceRequest(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            sharedAwake()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func poolPlayerView(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = SmallnessTransformable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.imageSoundFemale(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func objectAdd() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(appDestroy(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: app_statusKey,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(arrayCreate),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: show_successKey,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func appDestroy(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard ExamineedReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard ExamineedReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func arrayCreate() {
        //: ExamineedReactiveCompatible.share.loginUserMode.loungePlus = true
        ExamineedReactiveCompatible.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func read() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? SideCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? SideCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if WithCenterThen.beData(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.underLocal(tipStr: (String(userClickValue.suffix(4)) + " rea" + String(const_managerMessage.suffix(7)) + String(constToTitle)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension InfoThen: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func take() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        SwitchicialPermissionTool.blockText(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = MessageStreamPickTool.toSign(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.equalPhoto(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func discount() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        SwitchicialPermissionTool.aircraftAd(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.equalPhoto(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func equalPhoto(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: ExamineedReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            ExamineedReactiveCompatible.share.appStatus == InstanceHashableRepresentation.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = AspectModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.scoff(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = PublisherDataSource(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = AspectModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.scoff(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        sharedAwake()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func visualCommunication() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard WithCenterThen.bringFor(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == CommentRefCompatibleValue.male.rawValue ? "He".localized : "She".localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.heatherMixtureMoment(showMsg: (String(show_timeViewOfId.suffix(4)) + "s us" + String(userSendId.prefix(6)) + "o low " + String(appWithMakeTotalKey)).equalKey(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.loadPicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func loadPicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = MessageStreamPickTool.toSign(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            MessageStreamPickTool.outsideErase(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.overLine(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.heatherMixtureMoment(showMsg: (String(dataObserverMessage.prefix(6)) + "video at " + String(mainLabelId) + String(dataEditMessage.prefix(9))).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func overLine(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: SkipEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = PictureEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = AspectModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.conversationModel(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        CenterProgressHUD.showAcross()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        sharedAwake()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: SideCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func locationLive(msgData: SideCellData, sendModel: AspectModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        PicThen.shared.humankindCompletion(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.heatherMixtureMoment(showMsg: (String(show_modelPath) + String(noti_colorPassData)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.leftHandednessAlways(msgData, add: nil, pathFor: NSNumber(value: 0), statusProceedsChangeUse: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.conversationModel(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension InfoThen {
    //: func func__addVoiceNotification() {
    func blockBackground() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(progress),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(displayUp),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(valueBottomNotification(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(giftComponent(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: show_clickNetPath,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(deadlineNotification(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: showMeDisplayMessage,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func rrmove(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            ServerManagerDelegate.shared.glasses()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            ServerManagerDelegate.shared.viewDelegate()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func progress() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        rrmove(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func displayUp() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        rrmove(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func valueBottomNotification(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            rrmove(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            rrmove(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: DismissInstanceCellData, cellView: TalkingChatAudioMsgCell) {
    func phonationUser(cellData: DismissInstanceCellData, cellView: EasingMsgCell) {
        //: guard TalkingSocketManager.shared.isServer == false else {
        guard LoadSocketDelegate.shared.isServer == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.heatherMixtureMoment(showMsg: mainMagnitudeimateMessage)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? SideCellData)!.isKind(of: DismissInstanceCellData.self) == false {
            if (indexModel as? SideCellData)!.isKind(of: DismissInstanceCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! DismissInstanceCellData
            let audioModel = indexModel as! DismissInstanceCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = ViewValueInfoManager.lineUp(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        ServerManagerDelegate.shared.stopShow()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if ServerManagerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: "播放状态:\(ServerManagerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if ServerManagerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                ServerManagerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        ServerManagerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        ServerManagerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: SideCellData = self.msgView.uiMsgs.object(at: index) as! SideCellData
                let subModel: SideCellData = self.msgView.uiMsgs.object(at: index) as! SideCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: DismissInstanceCellData.self) {
                if subModel.isKind(of: DismissInstanceCellData.self) {
                    //: let audioModel = subModel as! DismissInstanceCellData
                    let audioModel = subModel as! DismissInstanceCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = ViewValueInfoManager.lineUp(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    ImageLoadReactiveCompatible.contiguous(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        ServerManagerDelegate.shared.itemAction(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func past() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.statusWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.positionExecuteView(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? SideCellData
                let indexModel = model as? SideCellData
                //: if indexModel?.isKind(of: DismissInstanceCellData.self) == true || indexModel?.isKind(of: CompartmentCellData.self) == true || indexModel?.isKind(of: LoadCellData.self) == true || indexModel?.isKind(of: StreetSmartCellData.self) == true {
                if indexModel?.isKind(of: DismissInstanceCellData.self) == true || indexModel?.isKind(of: CompartmentCellData.self) == true || indexModel?.isKind(of: LoadCellData.self) == true || indexModel?.isKind(of: StreetSmartCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.towardFirst()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.brandName()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.read()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func statusWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.inclose(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.restore(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.statusFreshman()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == ExamineedReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == ExamineedReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == ExamineedReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == ExamineedReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.instanceIn(userWrap)
    }

    //: func func__getFillDraftText() {
    func phoneSystem() {
        //: let userID = "c2c_"+targetId
        let userID = "c2c_" + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.equalSecond()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func pointOut() {
        //: let userID = "c2c_"+targetId
        let userID = "c2c_" + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension InfoThen {
    //: func func__TipsNotification() {
    func keepAnEyeOn() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(labelBind(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: main_barPath,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func labelBind(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: ReloadReactiveCompatible = userinfo["msg"] as! ReloadReactiveCompatible

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(true)
        self.msgView.scroll(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func underLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: appStyleTitleNameMessage.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: main_withPlayerUrl.map{$0^68}, encoding: .utf8)!: "tips"]
        //: var message: SideCellData?
        var message: SideCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TitleChatManager.smart(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = TitleChatManager.messageInfo(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(true)
        self.msgView.scroll(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func brandName() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        WithCenterThen.serviceHandler { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.underLocal(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func observerTo(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.underLocal(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func towardFirst() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        WithCenterThen.showHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.underLocal(tipStr: String(bytes: k_methodMessage.map{mediumEqual(background: $0)}, encoding: .utf8)!.equalKey(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func giftComponent(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: SideCellData = dict?["cellData"] as! SideCellData
        let cellData: SideCellData = dict?[(String(appImageData) + app_fileData.replacingOccurrences(of: "same", with: "a"))] as! SideCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(true)
        self.msgView.scroll(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension InfoThen: SupplementThen {
    //: func func__setReplyTipsView() {
    func draftCopy() {
        //: let key =  "\(ExamineedReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(ExamineedReactiveCompatible.share.loginUserMode.userID)" + (String(userPicStr.prefix(4)) + "vateChatT" + notiRangeFromData.replacingOccurrences(of: "interaction", with: "i") + String(appFileEventMessage.suffix(8))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && ExamineedReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && ExamineedReactiveCompatible.share.appStatus == InstanceHashableRepresentation.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.visualImage()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            banner(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            atFor()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func application(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        nameAppearModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        atFor()
    }

    //: func removeReplyTipsView() {
    func atFor() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            banner(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func clearAway() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: mainKeyMessage, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: show_licenseData - kRecordFormat, width: user_barText, height: show_licenseData)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: user_barText,
                                     //: height: ScreenHeight)
                                     height: show_licenseData)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func withDeviceRequest(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.announcement() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if ListThen.withShared().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: main_informationId, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: StreamViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: show_licenseData - kRecordFormat,
                                     //: width: ScreenWidth,
                                     width: user_barText,
                                     //: height: ScreenHeight)
                                     height: show_licenseData)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if ListThen.withShared().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: main_informationId, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension InfoThen {
    /// 创建UI
    //: private func createUI() {
    private func anyMakeUi() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(appKitName.suffix(7)) + String(mainLoadValue) + userWhiteName.lowercased() + "ation").localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func instanceAcrossConstraints() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kRecordFormat : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.areaRecordRadii(width: contentView.width,
                                        //: height: contentView.height,
                                        height: contentView.height,
                                        //: corners: [.topLeft, .topRight],
                                        corners: [.topLeft, .topRight],
                                        //: cornerRadii: .init(width: 8, height: 8))
                                        cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? dataRecordTitle : main_infoId)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + dataShowText)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? dataRecordTitle : main_infoId)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + dataShowText)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + dataShowText)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func dataTo() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.finishClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.nameOf()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.save(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                main_inviteMsg.smartViewButton(eventID: const_itemPath, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
