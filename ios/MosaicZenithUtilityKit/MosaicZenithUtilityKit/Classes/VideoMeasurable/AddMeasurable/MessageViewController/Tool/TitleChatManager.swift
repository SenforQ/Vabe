
//: Declare String Begin

/*: "msgType" :*/
fileprivate let showWithFormat:String = "msgTypeconnection if right video"

/*: "msgInfo" :*/
fileprivate let noti_dataName:String = "msgInfgift to succeed"
fileprivate let main_giftId:String = "greet"

/*: "nickname" :*/
fileprivate let show_rawTitle:[UInt8] = [0xdc,0xdb,0xd1,0xd9,0xdc,0xd3,0xdf,0xd7]

				private func insertObserver(number num: UInt8) -> UInt8 {
					return num ^ 178
				}

/*: "headPic" :*/
fileprivate let constTouchKey:[UInt8] = [0x9a,0x97,0x93,0x96,0xa2,0x9b,0x91]

				private func upHandleMake(now num: UInt8) -> UInt8 {
					return num ^ 242
				}

/*: "isGroup" :*/
fileprivate let data_selectedAddSnapKey:[Character] = ["i"]
fileprivate let notiCellName:String = "value tap target returnsGroup"

/*: "WantGift" :*/
fileprivate let noti_contentAwakeGiftUrl:[Character] = ["W","a","n","t","G","i","f","t"]

/*: "messageType" :*/
fileprivate let k_iconTotalUrl:String = "MESS"

/*: "contentType" :*/
fileprivate let dataTipWrapManagerValue:[Character] = ["c","o","n","t","e","n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let data_centerPath:String = "ImgMsfalse up true"
fileprivate let k_dataName:String = "image"

/*: "imageUri" :*/
fileprivate let noti_documentMessage:String = "imusere"
fileprivate let mainSinceModelText:String = "fit makeUri"

/*: "isBurn" :*/
fileprivate let const_groupPath:[Character] = ["i","s","B","u","r"]
fileprivate let constFirstKey:[Character] = ["n"]

/*: "giftId" :*/
fileprivate let constPlayerDeviceStr:String = "giftIdregister data"

/*: "params" :*/
fileprivate let appNumberData:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let data_sizeFormat:String = "audioLview will"
fileprivate let user_profileContent:[Character] = ["e","n","g","t","h"]

/*: "audioSandbox" :*/
fileprivate let show_photoPath:[Character] = ["a","u","d","i","o","S","a","n","d"]
fileprivate let app_upText:String = "bmediumx"

/*: "senderId" :*/
fileprivate let main_infoData:[Character] = ["s","e","n","d","e","r","I","d"]

/*: "AudioMsg" :*/
fileprivate let mainSendColorUrl:String = "Audioview model shared"
fileprivate let show_normalValue:String = "share self voice startMsg"

/*: "content" :*/
fileprivate let constPathStr:String = "to"
fileprivate let main_sharedKey:String = "onimagent"

/*: "TxtMsg" :*/
fileprivate let show_shareMsg:String = "size"
fileprivate let constBottomStr:String = "xtMsgheight position"

/*: "message" :*/
fileprivate let user_appValue:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let constAddName:String = "quparty"
fileprivate let kPathTitle:[Character] = ["t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let showLayerData:String = "quoteMtrue label else true true"

/*: "rYMsgType" :*/
fileprivate let userDataMsg:[Character] = ["r","Y","M","s"]
fileprivate let kEventId:[Character] = ["g","T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let mainViewBlockPath:String = "GJ:Sname case pic"
fileprivate let showRawMsg:String = "toGrobject mode content"
fileprivate let appWrapMsg:String = "TxtCustdata intimate bottom show table"
fileprivate let appModelFormat:[Character] = ["o","m"]

/*: "changeDirection" :*/
fileprivate let data_infoMessage:String = "chascreen"
fileprivate let kSelectedMsg:String = "ECTI"
fileprivate let show_nameText:[Character] = ["o","n"]

/*: "system" :*/
fileprivate let k_pickName:[Character] = ["s","y","s","t","e"]
fileprivate let data_transitionMsg:String = "selected"

/*: "icon_chats_pcr" :*/
fileprivate let kArrayId:String = "IC"
fileprivate let kPathMessage:String = "on_chalet male size"

/*: "status" :*/
fileprivate let userCornerNameViewValue:[Character] = ["s","t","a","t","u"]
fileprivate let constLabUrl:[Character] = ["s"]

/*: "msgIncome" :*/
fileprivate let mainFeatureKey:String = "msgIclick view style"
fileprivate let noti_actionResultEqualName:String = "E"

/*: "RecordRelatableTarget" :*/
fileprivate let const_cellLevelKey:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","S","t","a","t","u","s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let app_intervalUrl:String = "t"
fileprivate let userSingleName:String = "first self imageMsgT"
fileprivate let mainMakeKey:String = "text totedCo"

/*: "formatType" :*/
fileprivate let constSourcePath:String = "fcenterrmat"
fileprivate let const_dataMsg:[Character] = ["T","y","p","e"]

/*: "imageData" :*/
fileprivate let userSizePath:String = "imageDatcase extension"
fileprivate let app_tableFormat:String = "request"

/*: "VideoMsg" :*/
fileprivate let show_documentStr:[Character] = ["V","i","d"]
fileprivate let appViewPath:[Character] = ["e","o","M","s","g"]

/*: "userIntro" :*/
fileprivate let data_withMsg:[Character] = ["u","s","e","r","I","n","t","r","o"]

/*: "targetId" :*/
fileprivate let constQuoteMsg:String = "TA"
fileprivate let notiShadowMessage:String = "orientation log true model finishrgetId"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let dataEditKey:[Character] = ["y","y","y","y","-","M","M","-","d"]
fileprivate let user_monitorRequestUrl:String = "path labd HH:m"

/*: "sendTime" :*/
fileprivate let appSucceedRegularVideoMsg:String = "scontentn"
fileprivate let userTouchManagerValue:String = "dTimetrue item action self true"

/*: "isRead" :*/
fileprivate let appMediumFormat:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let appMenuAfterValue:String = "aoccur"
fileprivate let kTaskText:String = "ioDatatag title request male image"

/*: "audioUri" :*/
fileprivate let appLabelGiftFormat:String = "container"
fileprivate let noti_sizeId:String = "point model to float rowudioUri"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TitleChatManager.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum CourseOfActionInsetTarget: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - TitleChatManager

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class TitleChatManager: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func doingCurrent() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> SideCellData? {
    public class func smart(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> SideCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = messageInfo(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: CenterV2Listener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            CenterV2Listener.shared.remindMake(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> SideCellData? {
    public class func messageInfo(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> SideCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(showWithFormat.prefix(7)))] = "tips"
            //: let extraDict = ["extra": extralDict]
            let extraDict = ["extra": extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = overuse(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ReloadReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = DataModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? CellModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(noti_dataName.prefix(6)) + main_giftId.replacingOccurrences(of: "greet", with: "o"))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict["user"] = [
            //: "uid": ExamineedReactiveCompatible.share.loginUserMode.userID,
            "uid": ExamineedReactiveCompatible.share.loginUserMode.userID,
            //: "nickname": ExamineedReactiveCompatible.share.loginUserMode.nickname,
            String(bytes: show_rawTitle.map{insertObserver(number: $0)}, encoding: .utf8)!: ExamineedReactiveCompatible.share.loginUserMode.nickname,
            //: "headPic": ExamineedReactiveCompatible.share.loginUserMode.headPic,
            String(bytes: constTouchKey.map{upHandleMake(now: $0)}, encoding: .utf8)!: ExamineedReactiveCompatible.share.loginUserMode.headPic,
            //: "age": ExamineedReactiveCompatible.share.loginUserMode.age,
            "age": ExamineedReactiveCompatible.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = ["extra": tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = overuse(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(showWithFormat.prefix(7)))] as? String == "gift" {
            //: var data: SideCellData
            var data: SideCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(data_selectedAddSnapKey) + String(notiCellName.suffix(6)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ChatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = PlayerCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = PlayerCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DataModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(showWithFormat.prefix(7)))] as? String == (String(noti_contentAwakeGiftUrl)) {
            //: let data = CutCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CutCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DataModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(showWithFormat.prefix(7)))] as? String == "txt" {
            //: var data: SideCellData
            var data: SideCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(data_selectedAddSnapKey) + String(notiCellName.suffix(6)))] as? String == "1" { // 群聊
                //: data = GrownupCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = GrownupCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = LoadCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = LoadCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DataModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(showWithFormat.prefix(7)))] as? String == "img" {
            //: let data = CompartmentCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CompartmentCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DataModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(showWithFormat.prefix(7)))] as? String == "video" {
            //: let data = StreetSmartCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = StreetSmartCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DataModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(showWithFormat.prefix(7)))] as? String == "audio" {
            //: let data = DismissInstanceCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DismissInstanceCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DataModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral["gift"] != nil && (msgInfo[(k_iconTotalUrl.lowercased() + "ageType")] as? Int) == 4 {
            //: let data = LoadCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = LoadCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DataModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: SideCellData) -> SideCellData? {
    public class func playerData(withData cellData: SideCellData) -> SideCellData? {
        // 图片
        //: if let reCellData = cellData as? CompartmentCellData {
        if let reCellData = cellData as? CompartmentCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(PicMacroDefine.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(PicMacroDefine.circleBy())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(dataTipWrapManagerValue))] = (String(data_centerPath.prefix(5)) + k_dataName.replacingOccurrences(of: "image", with: "g"))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(showWithFormat.prefix(7)))] = "img"
            //: msgInfo["imageUri"] = filePath
            msgInfo[(noti_documentMessage.replacingOccurrences(of: "user", with: "ag") + String(mainSinceModelText.suffix(3)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(noti_dataName.prefix(6)) + main_giftId.replacingOccurrences(of: "greet", with: "o"))] = msgInfo

            //: params["type"] = "ImgMsg"
            params["type"] = (String(data_centerPath.prefix(5)) + k_dataName.replacingOccurrences(of: "image", with: "g"))
            //: params["pic"] = filePath
            params["pic"] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(const_groupPath) + String(constFirstKey))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(const_groupPath) + String(constFirstKey))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(constPlayerDeviceStr.prefix(6)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(constPlayerDeviceStr.prefix(6)))] = "0"
            }

            //: dic["params"] = params
            dic[(String(appNumberData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = messageInfo(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? StreetSmartCellData {
        if let reCellData = cellData as? StreetSmartCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? DismissInstanceCellData {
        if let reCellData = cellData as? DismissInstanceCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = ViewValueInfoManager.lineUp(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(data_sizeFormat.prefix(6)) + String(user_profileContent))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params["len"] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(show_photoPath) + app_upText.replacingOccurrences(of: "medium", with: "o"))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(show_photoPath) + app_upText.replacingOccurrences(of: "medium", with: "o"))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(showWithFormat.prefix(7)))] = "audio"
            //: msgInfo["senderId"] = ExamineedReactiveCompatible.share.loginUserMode.userID
            msgInfo[(String(main_infoData))] = ExamineedReactiveCompatible.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(noti_dataName.prefix(6)) + main_giftId.replacingOccurrences(of: "greet", with: "o"))] = msgInfo

            //: params["type"] = "AudioMsg"
            params["type"] = (String(mainSendColorUrl.prefix(5)) + String(show_normalValue.suffix(3)))
            //: dic["params"] = params
            dic[(String(appNumberData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = messageInfo(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? LoadCellData {
        if let reCellData = cellData as? LoadCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(constPathStr.replacingOccurrences(of: "to", with: "c") + main_sharedKey.replacingOccurrences(of: "image", with: "te"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(showWithFormat.prefix(7)))] = "txt"
            //: dic["msgInfo"] = msgInfo
            dic[(String(noti_dataName.prefix(6)) + main_giftId.replacingOccurrences(of: "greet", with: "o"))] = msgInfo
            //: params["type"] = "TxtMsg"
            params["type"] = (show_shareMsg.replacingOccurrences(of: "size", with: "T") + String(constBottomStr.prefix(5)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(user_appValue))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.tvMsg() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(constAddName.replacingOccurrences(of: "party", with: "o") + String(kPathTitle))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(showLayerData.prefix(6)) + "sgInfo")] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(appNumberData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = messageInfo(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> SideCellData? {
    public class func cornerMetadata(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> SideCellData? {
        //: var cellData: SideCellData?
        var cellData: SideCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic["extra"] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(noti_dataName.prefix(6)) + main_giftId.replacingOccurrences(of: "greet", with: "o"))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic["user"] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? DataModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == ExamineedReactiveCompatible.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == ExamineedReactiveCompatible.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == ExamineedReactiveCompatible.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == ExamineedReactiveCompatible.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(userDataMsg) + String(kEventId))] as? String == (String(mainViewBlockPath.prefix(4)) + "ess:Au" + String(showRawMsg.prefix(4)) + "eet:" + String(appWrapMsg.prefix(7)) + String(appModelFormat)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(data_infoMessage.replacingOccurrences(of: "screen", with: "n") + "geDir" + kSelectedMsg.lowercased() + String(show_nameText))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = errorType(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(showWithFormat.prefix(7)))] as? String == (String(k_pickName) + data_transitionMsg.replacingOccurrences(of: "selected", with: "m")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = DataModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? CellModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ReloadReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(constPathStr.replacingOccurrences(of: "to", with: "c") + main_sharedKey.replacingOccurrences(of: "image", with: "te"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = CompartmentCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CompartmentCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = StreetSmartCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = StreetSmartCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ChatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = PlayerCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PlayerCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = skinny(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic["extra"] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(noti_dataName.prefix(6)) + main_giftId.replacingOccurrences(of: "greet", with: "o"))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? AggregationModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = CutCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CutCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = DismissInstanceCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DismissInstanceCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? DismissInstanceCellData {
            if let audioCellData = cellData as? DismissInstanceCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = provenance(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = ImageLoadReactiveCompatible.infoSound(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = ImageLoadReactiveCompatible.conversation(decorticate(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = PaperThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?["title"] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(constPathStr.replacingOccurrences(of: "to", with: "c") + main_sharedKey.replacingOccurrences(of: "image", with: "te"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?["img"] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = NewspaperPublisherCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = GrownupCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = GrownupCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = LoadCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = LoadCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? AstatineModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict["icon"] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: ExamineedReactiveCompatible.share.loginUserMode.headPic ?? ""
                        ExamineedReactiveCompatible.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = AggregationModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(constPathStr.replacingOccurrences(of: "to", with: "c") + main_sharedKey.replacingOccurrences(of: "image", with: "te"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = DirectionModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = "all"
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (kArrayId.lowercased() + String(kPathMessage.prefix(6)) + "ts_pcr")
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.eachPlace(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(String(userCornerNameViewValue) + String(constLabUrl))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(mainFeatureKey.prefix(4)) + "ncom" + noti_actionResultEqualName.lowercased())] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(k_iconTotalUrl.lowercased() + "ageType")] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic["msgId"] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(const_cellLevelKey))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = RecordRelatableTarget(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(app_intervalUrl.uppercased() + String(userSingleName.suffix(4)) + "ransla" + String(mainMakeKey.suffix(5)) + "ntent")] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = RequestPermissionModel.message()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = RequestPermissionModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = ClearSendModel.video()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = ClearSendModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = TitleModel.will()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func errorType(_ extraDic: [String: Any]) -> CourseOfActionInsetTarget {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic["extra"] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(noti_dataName.prefix(6)) + main_giftId.replacingOccurrences(of: "greet", with: "o"))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? DataModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(constSourcePath.replacingOccurrences(of: "center", with: "o") + String(const_dataMsg))] as? String == "plain" {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(noti_documentMessage.replacingOccurrences(of: "user", with: "ag") + String(mainSinceModelText.suffix(3)))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(userSizePath.prefix(8)) + app_tableFormat.replacingOccurrences(of: "request", with: "a"))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(show_documentStr) + String(appViewPath)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = buttonCurrent(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(showWithFormat.prefix(7)))] as? String == "tips" || (dic?[(k_iconTotalUrl.lowercased() + "ageType")] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(constSourcePath.replacingOccurrences(of: "center", with: "o") + String(const_dataMsg))] as? String == "mf") && ((dic?[(k_iconTotalUrl.lowercased() + "ageType")] as? Int) == 15 || (dic?[(k_iconTotalUrl.lowercased() + "ageType")] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(showWithFormat.prefix(7)))] as? String == (String(data_withMsg)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(showWithFormat.prefix(7)))] as? String == (String(noti_contentAwakeGiftUrl)) || (msgInfo?[(k_iconTotalUrl.lowercased() + "ageType")] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func decorticate(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.eachPlace(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic["extra"] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(noti_dataName.prefix(6)) + main_giftId.replacingOccurrences(of: "greet", with: "o"))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?["user"] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(main_infoData))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == ExamineedReactiveCompatible.share.loginUserMode.userID {
        if senderId == ExamineedReactiveCompatible.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(main_infoData))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(constQuoteMsg.lowercased() + String(notiShadowMessage.suffix(6)))] = message.userID ?? (user?[(constQuoteMsg.lowercased() + String(notiShadowMessage.suffix(6)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(main_infoData))] = "\(msgInfo?["uid"] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(constQuoteMsg.lowercased() + String(notiShadowMessage.suffix(6)))] = "\(msgInfo?["uid"] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains("msgId") ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.eachPlace(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic["msgId"] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?["msgId"] = "\(customDic["msgId"] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?["msgId"] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(show_photoPath) + app_upText.replacingOccurrences(of: "medium", with: "o"))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(show_photoPath) + app_upText.replacingOccurrences(of: "medium", with: "o"))] = "\(msgInfo?[(String(show_photoPath) + app_upText.replacingOccurrences(of: "medium", with: "o"))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(data_sizeFormat.prefix(6)) + String(user_profileContent))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(data_sizeFormat.prefix(6)) + String(user_profileContent))] = "\(msgInfo?[(String(data_sizeFormat.prefix(6)) + String(user_profileContent))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(dataEditKey) + String(user_monitorRequestUrl.suffix(6)) + "m:ss")
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(appSucceedRegularVideoMsg.replacingOccurrences(of: "content", with: "e") + String(userTouchManagerValue.prefix(5)))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == ExamineedReactiveCompatible.share.loginUserMode.userID ? "1" : "0"
        info?[(String(appMediumFormat))] = senderId == ExamineedReactiveCompatible.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func provenance(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = decorticate(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info["msgId"] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func skinny(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func overuse(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func buttonCurrent(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(showWithFormat.prefix(7)))] as? String, msgType == "audio" {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(dataTipWrapManagerValue))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(mainSendColorUrl.prefix(5)) + String(show_normalValue.suffix(3)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(appMenuAfterValue.replacingOccurrences(of: "occur", with: "ud") + String(kTaskText.prefix(6)))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(appLabelGiftFormat.replacingOccurrences(of: "container", with: "a") + String(noti_sizeId.suffix(7)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
