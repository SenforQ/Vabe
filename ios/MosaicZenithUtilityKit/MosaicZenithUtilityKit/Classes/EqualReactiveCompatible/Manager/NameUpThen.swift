
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let noti_giftTransformPath:String = "setPasleading with value label"
fileprivate let user_modelTitle:[Character] = ["s","w","o","r","d"]

/*: "user/appleLogin" :*/
fileprivate let k_roundMsg:String = "USER"
fileprivate let user_indexPath:String = "leLogincorner cell target view"

/*: "user/googleLogin" :*/
fileprivate let app_succeedFormat:String = "usequalr"
fileprivate let userSizeTitle:String = "brow"
fileprivate let dataHiddenName:String = "click in button aseLogin"

/*: "user/smsLogin" :*/
fileprivate let constIndexStr:String = "usedge"
fileprivate let app_equalNextOfData:String = "geffect"

/*: "user/emailVcodeLogin" :*/
fileprivate let k_sexMessage:String = "user/etouch color name function return"
fileprivate let kFemaleRecordTitle:String = "CODE"
fileprivate let showGiftPlayerData:String = "Loginif equal"

/*: "user/pwdLogin" :*/
fileprivate let dataSourceValue:String = "push pageuser/pw"
fileprivate let noti_userValue:String = "blind image life datadLogin"

/*: "user/emailPwdLogin" :*/
fileprivate let const_wrapValue:String = "USER"
fileprivate let noti_sharedStr:String = "ilPwreturn make title component"
fileprivate let dataTitleKey:String = "hidden"

/*: "mobile" :*/
fileprivate let show_userMakeMessage:[UInt8] = [0x65,0x6c,0x69,0x62,0x6f,0x6d]

/*: "areaCode" :*/
fileprivate let app_indexColorTitle:[UInt8] = [0x69,0x7a,0x6d,0x69,0x4b,0x67,0x6c,0x6d]

				private func sizeTo(image num: UInt8) -> UInt8 {
					return num ^ 8
				}

/*: "user/sendLoginSms" :*/
fileprivate let notiAppStyleTitle:String = "counter"
fileprivate let show_managerKey:String = "LoginSmsview view if self application"

/*: "RSA加密失败" :*/
fileprivate let main_emptyPlayerSectionKey:String = "RSA加密失\u{8d25}"

/*: "password" :*/
fileprivate let show_numberTitle:[UInt8] = [0x6d,0x7c,0x6e,0x6e,0x6a,0x72,0x6f,0x79]

				private func recordAlbum(last num: UInt8) -> UInt8 {
					return num ^ 29
				}

/*: "user/modifyPassword" :*/
fileprivate let show_cornerKey:[Character] = ["u","s","e","r","/","m","o","d","i","f","y","P","a","s","s"]
fileprivate let constMyMsg:String = "screenrd"

/*: "user/sendModifyPwdSms" :*/
fileprivate let mainModelTitle:String = "useelement"
fileprivate let user_succeedFrameMessage:[Character] = ["M","o","d","i","f","y","P","w","d","S","m","s"]

/*: "user/sendModifyPwdEmail" :*/
fileprivate let appContactName:String = "usbottom"
fileprivate let appPicValue:String = "make model model normalModify"
fileprivate let const_labButtonValue:[Character] = ["P","w","d","E","m","a","i","l"]

/*: "mf/user/getMyInfo" :*/
fileprivate let k_errorDataFormat:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","M","y","I","n","f","o"]

/*: "invite/bindInviter" :*/
fileprivate let noti_titleUrl:String = "progressnvprogress"
fileprivate let app_userName:[Character] = ["I","n","v","i","t","e","r"]

/*: "invite/isBind" :*/
fileprivate let notiShareMsg:String = "invitmanager"
fileprivate let kInfoSnapFormat:[Character] = ["/","i","s","B","i","n","d"]

/*: "mf/user/CheckInviteCode" :*/
fileprivate let k_sexText:[Character] = ["m","f"]
fileprivate let kWhiteContent:String = "begin as cell/user"
fileprivate let showManagerViewId:String = "label complete indexkInvit"

/*: "mf/user/updateInfo" :*/
fileprivate let mainButtonCornerInputName:String = "label count small editmf/us"
fileprivate let app_makeDataTitle:String = "er/updamodel index if no"
fileprivate let dataImageMessage:String = "teInfomaximum lab button text"

/*: "user/getEmailBindVCode" :*/
fileprivate let mainEqualButtonKey:String = "key manageruser/"
fileprivate let kAppValue:String = "language place selfgetE"
fileprivate let showViewCenterStr:String = "indVCodeof view height warning data"

/*: "user/sendLoginEmail" :*/
fileprivate let userRawId:String = "user/stype else"
fileprivate let userResultMessage:String = "gigreet"
fileprivate let userMePath:String = "Emailvalue mode i"

/*: "user/emailBind" :*/
fileprivate let showSendName:[Character] = ["u","s","e","r","/","e","m","a","i","l","B","i","n"]
fileprivate let user_clickTitle:[Character] = ["d"]

/*: "user/logout" :*/
fileprivate let dataHiddenUserKey:String = "user/loarray format total equal"
fileprivate let show_miracleText:String = "GOUT"

/*: "app/countryList" :*/
fileprivate let main_levelValue:String = "pull self type shrink varapp/c"
fileprivate let constEqualName:String = "sto"

/*: "user/getMobileBindVCode" :*/
fileprivate let appCellFormat:String = "user/gfront ease corner"
fileprivate let app_sectionValue:String = "text stopbileB"

/*: "user/mobileBind" :*/
fileprivate let appPlayerId:[Character] = ["u","s","e","r","/","m","o","b","i"]
fileprivate let user_dataWhiteColorName:String = "leBindas server let class lab"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NameUpThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class NameUpThen: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func shared(type: RequestModelKeyRepresentable, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.loginParams(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(String(noti_giftTransformPath.prefix(6)) + String(user_modelTitle))].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: AppPushManager.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                AppPushManager.share.textPwd(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            NameUpThen.pestle(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingRequestModel {
    private class func loginParams(type: RequestModelKeyRepresentable, params: [String: String]) -> VoiceRequestModel {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (k_roundMsg.lowercased() + "/app" + String(user_indexPath.prefix(7)))
        //: case .GoogleLogin:
        case .GoogleLogin:
            //: reqModel.requestPath = "user/googleLogin"
            reqModel.requestPath = (app_succeedFormat.replacingOccurrences(of: "equal", with: "e") + "/goo" + userSizeTitle.replacingOccurrences(of: "brow", with: "gl") + String(dataHiddenName.suffix(6)))
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (constIndexStr.replacingOccurrences(of: "edge", with: "er") + "/smsLo" + app_equalNextOfData.replacingOccurrences(of: "effect", with: "in"))
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (String(k_sexMessage.prefix(6)) + "mailV" + kFemaleRecordTitle.lowercased() + String(showGiftPlayerData.prefix(5)))
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (String(dataSourceValue.suffix(7)) + String(noti_userValue.suffix(6)))
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (const_wrapValue.lowercased() + "/ema" + String(noti_sharedStr.prefix(4)) + "dLogi" + dataTitleKey.replacingOccurrences(of: "hidden", with: "n"))
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func completionNow(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: show_keyMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: show_userMakeMessage.reversed(), encoding: .utf8)!: reslutStr, String(bytes: app_indexColorTitle.map{sizeTo(image: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = VoiceRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (notiAppStyleTitle.replacingOccurrences(of: "count", with: "us") + "/send" + String(show_managerKey.prefix(8)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (main_emptyPlayerSectionKey))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func visualizationCompletion(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: show_keyMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: show_userMakeMessage.reversed(), encoding: .utf8)!: resultPhoneStr, String(bytes: app_indexColorTitle.map{sizeTo(image: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: show_numberTitle.map{recordAlbum(last: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            NameUpThen.shared(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (main_emptyPlayerSectionKey))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func make(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: show_keyMessage)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: show_numberTitle.map{recordAlbum(last: $0)}, encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params["vcode"] = vcode
            }

            //: let reqModel = TalkingRequestModel()
            let reqModel = VoiceRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(show_cornerKey) + constMyMsg.replacingOccurrences(of: "screen", with: "wo"))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (main_emptyPlayerSectionKey))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func move(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (mainModelTitle.replacingOccurrences(of: "element", with: "r") + "/send" + String(user_succeedFrameMessage))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func video(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (appContactName.replacingOccurrences(of: "bottom", with: "er") + "/send" + String(appPicValue.suffix(6)) + String(const_labButtonValue))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func pestle(_ type: RequestModelKeyRepresentable, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(k_errorDataFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            const_appStr.set(result, forKey: kMaxData)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<CullOutHandyJSON>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: ExamineedReactiveCompatible.share.loginUserMode = userModel
                ExamineedReactiveCompatible.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                const_appStr.set(type.rawValue, forKey: notiMessageData)
                //: if ExamineedReactiveCompatible.share.loginUserMode.updateInfo == false {
                if ExamineedReactiveCompatible.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: AppPushManager.share.func__pushToFillUserInfoVC()
                    AppPushManager.share.needEnd()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: userPartyVersionValue, object: nil, userInfo: result as! [String: Any])

                //: if ExamineedReactiveCompatible.share.loginUserMode.remindBindEmail == true {
                if ExamineedReactiveCompatible.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: AppPushManager.share.func__pushToLockUserEmailVC(isShowBack: false)
                        AppPushManager.share.deadlineShared(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func wall(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (noti_titleUrl.replacingOccurrences(of: "progress", with: "i") + "te/bind" + String(app_userName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 是否已经绑定过邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_isBindInviter(completion: @escaping FinishBlock) {
    class func dataConverter(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "invite/isBind"
        reqModel.requestPath = (notiShareMsg.replacingOccurrences(of: "manager", with: "e") + String(kInfoSnapFormat))

        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func targetView(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(k_sexText) + String(kWhiteContent.suffix(5)) + "/Chec" + String(showManagerViewId.suffix(6)) + "eCode")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func sizeLog(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(mainButtonCornerInputName.suffix(5)) + String(app_makeDataTitle.prefix(7)) + String(dataImageMessage.prefix(6)))
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.toDate()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(show_clickStatusMessage)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.encryptLengthKey(key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func text(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: show_keyMessage)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = ["email": reslutStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = VoiceRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (String(mainEqualButtonKey.suffix(5)) + String(kAppValue.suffix(4)) + "mailB" + String(showViewCenterStr.prefix(8)))
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (String(userRawId.prefix(6)) + "endLo" + userResultMessage.replacingOccurrences(of: "greet", with: "n") + String(userMePath.prefix(5)))
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (main_emptyPlayerSectionKey))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func beforeReply(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (String(showSendName) + String(user_clickTitle))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = ["vcode": emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func record(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: show_keyMessage)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = ["email": resultEmailStr, String(bytes: show_numberTitle.map{recordAlbum(last: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            NameUpThen.shared(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (main_emptyPlayerSectionKey))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func login(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (String(dataHiddenUserKey.prefix(7)) + show_miracleText.lowercased())
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func ofItem(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(k_errorDataFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                const_appStr.set(result, forKey: kMaxData)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<CullOutHandyJSON>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: ExamineedReactiveCompatible.share.loginUserMode = userModel
                    ExamineedReactiveCompatible.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func belowProcess(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(main_levelValue.suffix(5)) + "ountryLi" + constEqualName.replacingOccurrences(of: "to", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func colorSub(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: show_keyMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: show_userMakeMessage.reversed(), encoding: .utf8)!: reslutStr, String(bytes: app_indexColorTitle.map{sizeTo(image: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = VoiceRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (String(appCellFormat.prefix(6)) + "etMo" + String(app_sectionValue.suffix(5)) + "indVCode")
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (main_emptyPlayerSectionKey))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func show(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: show_keyMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: show_userMakeMessage.reversed(), encoding: .utf8)!: reslutStr, String(bytes: app_indexColorTitle.map{sizeTo(image: $0)}, encoding: .utf8)!: reslutCodeStr, "vcode": vcode]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = VoiceRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (String(appPlayerId) + String(user_dataWhiteColorName.prefix(6)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (main_emptyPlayerSectionKey))
        }
    }
}
