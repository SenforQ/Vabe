
//: Declare String Begin

/*: "cancelCall" :*/
fileprivate let constDirectionKeyIfStr:String = "cancelCattention let image event"
fileprivate let dataSizeText:String = "aviewview"

/*: "#322870" :*/
fileprivate let showValueMessage:[Character] = ["#","3","2","2","8","7"]
fileprivate let app_demonstrateTitle:String = "0"

/*: "Tap to start video chatting" :*/
fileprivate let app_backId:[Character] = ["T","a","p"," ","t","o"," ","s","t","a","r","t"," ","v","i","d","e","o"," ","c","h","a","t","t","i"]
fileprivate let const_showData:String = "sharedg"

/*: "icon_close_nor" :*/
fileprivate let appMainPath:String = "icon_closrequest show user item make"
fileprivate let mainSourceMsg:String = "e_noras icon temp"

/*: "icon_views_number" :*/
fileprivate let data_phoneTitle:String = "icon_main let request live"
fileprivate let showAppVideoSharedMessage:[Character] = ["v","i","e","w","s"]
fileprivate let data_labTextStr:[Character] = ["_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let dataColorIconLabPath:[Character] = ["%","@"," ","c","h"]
fileprivate let userRequestContent:[Character] = ["a"]
fileprivate let dataSexId:String = "equalequaling"

/*: "icon_lounge" :*/
fileprivate let noti_textName:String = "icon_lsize view text self shared"
fileprivate let userColorData:[Character] = ["o","u","n","g","e"]

/*: "icon_rank_coin" :*/
fileprivate let k_resultName:[Character] = ["i","c","o","n","_","r","a","n","k","_","c","o"]
fileprivate let appViewStr:String = "text"

/*: "vip_btn_top" :*/
fileprivate let show_toText:[Character] = ["v","i","p","_","b","t","n","_","t","o"]
fileprivate let kCenterUrl:String = "tool"

/*: "btn_nearby_settings_nor" :*/
fileprivate let notiExamineedData:String = "appear"
fileprivate let constCellTitle:[Character] = ["t","n","_","n","e","a","r","b","y","_","s","e","t","t","i","n","g","s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let k_imageIconFormat:String = "title true%@ Coins/"
fileprivate let appViewId:String = "cloudn"

/*: "get json error" :*/
fileprivate let const_collectionTitle:String = "table"
fileprivate let dataVoiceMsg:[Character] = ["t"," ","j","s","o","n"," ","e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let kEmptyStr:String = "text managermatchId"

/*: "VIPFee" :*/
fileprivate let data_beautyPath:String = "instance try with name trueVIPFee"

/*: "Matching now" :*/
fileprivate let noti_headUrl:String = "Matchif record"
fileprivate let k_popMessage:String = "value bottom change color typeing now"

/*: "userTouch" :*/
fileprivate let userDomainTitle:String = "error show to make statususerT"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let app_changeClearMessage:String = "add varmf/fr"
fileprivate let show_nameLogValue:String = "ll/gfoundation view sub random"
fileprivate let const_makeText:[Character] = ["f","o"]

/*: "fromType" :*/
fileprivate let app_showName:[UInt8] = [0x9f,0xab,0xa8,0xa6,0x8d,0xb2,0xa9,0x9e]

				fileprivate func subBlock(model num: UInt8) -> UInt8 {
					let value = Int(num) - 57
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "You've been barred from receiving calls" :*/
fileprivate let user_imageKey:[UInt8] = [0xdc,0xf2,0xf8,0xaa,0xf9,0xe8,0xa3,0xe5,0xe8,0xe8,0xf1,0xa3,0xe5,0xe4,0xf5,0xf5,0xe8,0xe7,0xa3,0xe9,0xf5,0xf2,0xf0,0xa3,0xf5,0xe8,0xe6,0xe8,0xec,0xf9,0xec,0xf1,0xea,0xa3,0xe6,0xe4,0xef,0xef,0xf6]

				fileprivate func plusStatus(error num: UInt8) -> UInt8 {
					let value = Int(num) - 131
					if value < 0 {
						return UInt8(value + 256)
					} else {
						return UInt8(value)
					}
				}

/*: "source" :*/
fileprivate let user_progressId:[UInt8] = [0x44,0x40,0x46,0x43,0x34,0x36]

				fileprivate func magnitudeName(make num: UInt8) -> UInt8 {
					let value = Int(num) + 47
					if value > 255 {
						return UInt8(value - 256)
					} else {
						return UInt8(value)
					}
				}

/*: "fromFreeCall" :*/
fileprivate let main_actualValue:[Character] = ["f"]
fileprivate let show_rawTitle:String = "romFpin woman model color user"

/*: "requestCall" :*/
fileprivate let user_showUpgradeName:String = "REQU"

/*: "onVideoMatch" :*/
fileprivate let dataMixtureSnapName:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let k_buttonName:[UInt8] = [0x38,0x22,0x16,0x17,0xc,0xe,0x2,0x17,0xa,0x0,0x43,0x15,0xa,0x7,0x6,0xc,0x3e,0x43,0x31,0x6,0x0,0x6,0xa,0x15,0x6,0x43,0x15,0xa,0x7,0x6,0xc,0x43,0x0,0x2,0xf,0xf,0x43,0xa,0xd,0x0,0xc,0xe,0xa,0xd,0x4]

/*: "refuseCall" :*/
fileprivate let constUpId:String = "refeasing"
fileprivate let appBottomValue:String = "ofof"

/*: "onCancelCall" :*/
fileprivate let kWhiteViewUrl:String = "let var inonCance"
fileprivate let mainTargetMsg:[Character] = ["l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let kWithErrorTitle:[Character] = ["r","e","t","r","y"]
fileprivate let data_nowFormat:[Character] = ["A","f","t","e","r"]

/*: "onRequestCall" :*/
fileprivate let mainDataTimeFormat:String = "onRefrom equal to continue var"
fileprivate let appSizeMsg:String = "jumpjump"

/*: "failType" :*/
fileprivate let noti_makeFormat:String = "bottom center add voicefailT"
fileprivate let data_countId:[Character] = ["y","p","e"]

/*: "callWaitingTime" :*/
fileprivate let k_textFormat:String = "if label path self logcallWa"
fileprivate let mainDataKey:[Character] = ["i","t","i","n","g","T","i","m","e"]

/*: "Free 1 min" :*/
fileprivate let noti_viewLabMessage:String = "Free 1 madd to text cell return"
fileprivate let constStrengthStr:String = "bring"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AddViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class AddViewController: ReloadViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.cloutPrice()
            }
        }
        //: self.resetCamera()
        self.createToCamera()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        addSubviews()
        //: setupSubViewsConstraint()
        controlConstraint()
        //: bindInteraction()
        heritageQuantityeraction()
        //: videoManager.startPreview(isMatch: true)
        videoManager.jibe(isMatch: true)
        //: setPlayer(isRandon: false)
        collectionRandon(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            coequalVideo()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            LoadSocketDelegate.shared.greetButton(info: ["cmd": (String(constDirectionKeyIfStr.prefix(7)) + dataSizeText.replacingOccurrences(of: "view", with: "l")), "data": ["logId": logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        waiting()
        //: if TalkingSocketManager.shared.isServer == false {
        if LoadSocketDelegate.shared.isServer == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            LoadSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            LoadSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            LoadSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            LoadSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ModeChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = ModeChatManager()
        //: let info = TalkingVideoChatModel()
        let info = HumankindChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .equalTarget()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(coequalVideo), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: user_barText + 1, height: show_licenseData + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(showValueMessage) + app_demonstrateTitle.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(app_backId) + const_showData.replacingOccurrences(of: "shared", with: "n")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.snapIcon(name: (String(appMainPath.prefix(9)) + String(mainSourceMsg.prefix(5)))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(statusUp), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: GroupVideoControl = {
        //: let btn = TalkingButton()
        let btn = GroupVideoControl()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.snapIcon(name: (String(data_phoneTitle.prefix(5)) + String(showAppVideoSharedMessage) + String(data_labTextStr))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.snapIcon(name: (String(data_phoneTitle.prefix(5)) + String(showAppVideoSharedMessage) + String(data_labTextStr))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.font(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(dataColorIconLabPath) + String(userRequestContent) + dataSexId.replacingOccurrences(of: "equal", with: "t")).equalKey(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.snapIcon(name: (String(noti_textName.prefix(6)) + String(userColorData)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.snapIcon(name: (String(k_resultName) + appViewStr.replacingOccurrences(of: "text", with: "in")))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.font(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.snapIcon(name: (String(show_toText) + kCenterUrl.replacingOccurrences(of: "tool", with: "p")))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: GroupVideoControl = {
        //: let btn = TalkingButton()
        let btn = GroupVideoControl()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(cellPrice), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.snapIcon(name: (notiExamineedData.replacingOccurrences(of: "appear", with: "b") + String(constCellTitle))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.snapIcon(name: (String(k_resultName) + appViewStr.replacingOccurrences(of: "text", with: "in")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .utilizerForBodyCovering()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(k_imageIconFormat.suffix(9)) + appViewId.replacingOccurrences(of: "cloud", with: "mi")).equalKey(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.font(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: SectionView = //: return SectionView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension AddViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func collectionRandon(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = ValueReactiveCompatible.default.express(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (const_collectionTitle.replacingOccurrences(of: "table", with: "ge") + String(dataVoiceMsg)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func disoblige(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(kEmptyStr.suffix(7))): self.matchId,
                      //: "cmd": cmd,
                      "cmd": cmd,
                      //: "errno": errno] as [String: Any]
                      "errno": errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ColorReactiveCompatible.sizeAcross(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.halt()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(kEmptyStr.suffix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func dateWithFee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ColorReactiveCompatible.noneEqual { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(data_beautyPath.suffix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.cloutPrice()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension AddViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func cellPrice() {
        //: AppPushManager.share.func__pushToSubscribeAlert()
        AppPushManager.share.commentAdd()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func statusUp() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        conversationAcross()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        main_inviteMsg.smartViewButton(eventID: user_buttonName)

        //: if ExamineedReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if ExamineedReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.byContent()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        announcement()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func coequalVideo() {
        //: guard TalkingSocketManager.shared.isServer == false else {
        guard LoadSocketDelegate.shared.isServer == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.heatherMixtureMoment(showMsg: mainMagnitudeimateMessage)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        magnitudeernalRepresentation()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(noti_headUrl.prefix(5)) + String(k_popMessage.suffix(7))).localized
        //: setPlayer(isRandon: true)
        collectionRandon(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if ExamineedReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
        if ExamineedReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            main_inviteMsg.smartViewButton(eventID: kErrVoiceMsg)
            //: req_getVideoInfo()
            userTarget()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            main_inviteMsg.smartViewButton(eventID: user_pageAddId)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            LoadSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            LoadSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            LoadSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            LoadSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            maleTo()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func maleTo(cmd: String = (String(userDomainTitle.suffix(5)) + "ouch"), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        waiting()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        disoblige(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func halt() {
        // 停止定时器
        //: destroyWaitingTimer()
        waiting()
        //: showCloseBtn()
        conversationAcross()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(app_backId) + const_showData.replacingOccurrences(of: "shared", with: "n")).localized
        //: setPlayer(isRandon: false)
        collectionRandon(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isServer == false {
        if LoadSocketDelegate.shared.isServer == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            LoadSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            LoadSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            LoadSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            LoadSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        cloutPrice()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func waiting() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func userTarget() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = VoiceRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(app_changeClearMessage.suffix(5)) + "eeCa" + String(show_nameLogValue.prefix(4)) + "etVideoIn" + String(const_makeText))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: app_showName.map{subBlock(model: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataFilterMsg.makeBottom(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.halt()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: ExamineedReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    ExamineedReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.coequalVideo()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.heatherMixtureMoment(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = DismissMeasurable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = InstancePlayListener()
                //: player.setMute(bEnable: true)
                player.sumEnable(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.upholdQuantityerest(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                OverlookManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                SwitchicialPermissionTool.motionPictureCamera { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.superposeCornerThroughWithStreetwise()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func superposeCornerThroughWithStreetwise() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        main_inviteMsg.smartViewButton(eventID: kNeedUrl)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard OverlookManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.heatherMixtureMoment(showMsg: String(bytes: user_imageKey.map{plusStatus(error: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            OverlookManager.shared.dismissPoint(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.halt()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = VideoEqualVc()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.announcement()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.halt()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func magnitudeernalRepresentation() {
        //: if ExamineedReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if ExamineedReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(ExamineedReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(ExamineedReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func conversationAcross() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func createToCamera() {
        //: guard TalkingSocketManager.shared.isServer == false else {
        guard LoadSocketDelegate.shared.isServer == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.jibe()
        }
    }
}

// MARK: - EliteMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension AddViewController: EliteMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func beToData(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = SkipMeasurable.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(kEmptyStr.suffix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: user_progressId.map{magnitudeName(make: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       "type": model.type,
                                       //: "uid": model.uid]
                                       "uid": model.uid]
        //: let index = EnableFreeCallType.randow
        let index = DisappearScriptExpressionConvertible.randow
        //: if ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && ExamineedReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && ExamineedReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(main_actualValue) + String(show_rawTitle.prefix(4)) + "reeCall"))
            //: TalkingSocketManager.shared.isFreeCall = true
            LoadSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = ["cmd": (user_showUpgradeName.lowercased() + "estCall"), "data": callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        LoadSocketDelegate.shared.greetButton(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            LoadSocketDelegate.shared.greetButton(info: ["cmd": (String(constDirectionKeyIfStr.prefix(7)) + dataSizeText.replacingOccurrences(of: "view", with: "l")), "data": ["logId": self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.maleTo(cmd: (String(dataMixtureSnapName)))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func projectionData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json["logId"].intValue
    }
}

// MARK: - HostObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension AddViewController: HostObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func socket(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func monologuize(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func sovietSocialistRepublic(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func financialAid(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            DataTapThen.change(body: String(bytes: k_buttonName.map{$0^99}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = SkipMeasurable.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            licenseViewUid(toUid: String(uid))
        }
        //: stop_randomVideo()
        halt()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.shouldReleaseAllResource()
        //: ExamineedReactiveCompatible.share.start1v1TalkCall(info: data, situation: .Calling)
        ExamineedReactiveCompatible.share.taskSituation(info: data, situation: .Calling)
    }
}

// MARK: - TitleObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension AddViewController: TitleObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func betweenText(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        maleTo(cmd: (constUpId.replacingOccurrences(of: "easing", with: "u") + "seCa" + appBottomValue.replacingOccurrences(of: "of", with: "l")))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func tagData(data _: [String: Any]) {}
}

// MARK: - DistrictManagerErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension AddViewController: DistrictManagerErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func dataFeature(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(kWhiteViewUrl.suffix(7)) + String(mainTargetMsg)) { // 取消通话异常
            //: stop_randomVideo()
            halt()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(dataMixtureSnapName)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            halt()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(kEmptyStr.suffix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.heatherMixtureMoment(showMsg: msg)
            //: uploadFailTime(type: 2)
            toolFinish(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(dataMixtureSnapName)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(kWithErrorTitle) + String(data_nowFormat))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(kEmptyStr.suffix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.maleTo(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(mainDataTimeFormat.prefix(4)) + "questCa" + appSizeMsg.replacingOccurrences(of: "jump", with: "l")), errorNo == PathEquatable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            halt()
            //: guard ExamineedReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard ExamineedReactiveCompatible.share.appStatus == InstanceHashableRepresentation.normal.rawValue else { return }
            //: AppPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            AppPushManager.share.betweenArray(clickEvent: userViewCleanUrl, sufficient: false)
            //: uploadFailTime(type: 1)
            toolFinish(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        maleTo(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension AddViewController {
    //: func uploadFailTime(type: Int) {
    func toolFinish(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(noti_makeFormat.suffix(5)) + String(data_countId))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(k_textFormat.suffix(6)) + String(mainDataKey))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        main_inviteMsg.m(eventID: k_detailMessage, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func licenseViewUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(k_textFormat.suffix(6)) + String(mainDataKey))] = time
        //: parm["toUid"] = toUid
        parm["toUid"] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        main_inviteMsg.m(eventID: kInfoName, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension AddViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func cloutPrice() {
        //: guard ExamineedReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard ExamineedReactiveCompatible.share.appStatus == InstanceHashableRepresentation.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !ExamineedReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !ExamineedReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = DisappearScriptExpressionConvertible.randow
        //: if ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && ExamineedReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if ExamineedReactiveCompatible.share.loginUserMode.freeCallTimes > 0, ExamineedReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if ExamineedReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if ExamineedReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(noti_viewLabMessage.prefix(8)) + constStrengthStr.replacingOccurrences(of: "bring", with: "in")).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = ExamineedReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = ExamineedReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = "%@/min".equalKey(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: "%@/min".equalKey(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.dismissSharedSize(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = ExamineedReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = ExamineedReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = ExamineedReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = ExamineedReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !ExamineedReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !ExamineedReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func addSubviews() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func controlConstraint() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(app_versionItemStr + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func heritageQuantityeraction() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(cloutPrice),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: main_marginName,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(dateWithFee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: mainOnKey,
                                               //: object: nil)
                                               object: nil)
    }
}
